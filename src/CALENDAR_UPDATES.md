# Обновления календарей - Категории и улучшенные фильтры

## Обзор изменений

Добавлены улучшения в оба календаря:
1. **Календарь проекта** - структуризация по категориям + фильтр категорий
2. **Календарь дашборда** - правильное разделение личных и проектных задач

---

## 📅 Календарь проекта

### ✨ Новые возможности

#### 1. Структуризация по категориям (Swimlanes)

Задачи теперь отображаются **группами по категориям**, где каждая категория - это отдельная "дорожка" (swimlane):

**Преимущества:**
- 🎯 **Визуальная группировка** - сразу видно какие задачи относятся к какой категории
- 📊 **Счетчики** - видно сколько задач в каждой категории
- 🎨 **Цветовые индикаторы** - каждая категория помечена своим цветом
- 📌 **Sticky колонка** - названия категорий всегда видны при горизонтальной прокрутке

**Структура:**
```
┌──────────┬─────────────────────────────────────┐
│ Категория│         Временная шкала             │
├──────────┼─────────────────────────────────────┤
│ ● Дизайн │ ████ Задача 1    ████ Задача 2     │
│ 3 задачи │     ██████ Задача 3                 │
├──────────┼─────────────────────────────────────┤
│ ● Разраб │       ████████ Задача 4             │
│ 1 задача │                                     │
├──────────┼─────────────────────────────────────┤
│ ⚪ Без   │                 ██████ Задача 5     │
│ категор. │                                     │
└──────────┴─────────────────────────────────────┘
```

#### 2. Фильтр по категориям

Добавлен новый фильтр для выбора категорий:

**Особенности:**
- 🏷️ Кнопка **"Категории"** с иконкой Tag
- ✅ **Мультивыбор** - можно выбрать несколько категорий
- 📊 **Badge со счетчиком** - показывает сколько категорий выбрано
- 🎯 Опция **"Без категории"** - для задач без категории
- 🔘 Кнопки **"Все"** и **"Сброс"** для быстрого управления

**Интерфейс фильтра:**
```
┌────────────────────────┐
│ Категории      Все Сброс│
├────────────────────────┤
│ ☑ ● Дизайн            │
│ ☑ ● Разработка        │
│ ☐ ● Тестирование      │
├────────────────────────┤
│ ☑ ⚪ Без категории    │
└────────────────────────┘
```

#### 3. Задачи без категории

Специальная обработка задач без категории:

- 📌 Отдельная группа **"Без категории"**
- ⚪ Серый индикатор (вместо цветного)
- 🔽 Всегда отображается **внизу списка** категорий
- 🎯 Можно фильтровать отдельно

### 🎨 Визуальные улучшения

1. **Левая колонка с категориями:**
   - Ширина: 160px (40 * 4)
   - Sticky позиционирование
   - Цветной индикатор + название
   - Счетчик задач

2. **Высота категории:**
   - Динамическая (зависит от количества рядов задач)
   - Минимум: 40px
   - Расчет: `количество_рядов × 60px`

3. **Цветовая схема:**
   - Категории: фиолетовый, зеленый, синий, розовый, оранжевый, красный, желтый, индиго
   - Без категории: серый (bg-gray-400)

### 📊 Логика отображения

```typescript
// Группировка по категориям
const categoryGroups: CategoryGroup[] = [];

// Для каждой категории создаем группу
tasksByCategory.forEach((tasks, categoryId) => {
  const categoryName = categoryId 
    ? categories.find(c => c.id === categoryId)?.name 
    : 'Без категории';
    
  const categoryColor = categoryId
    ? getColorClass(categories.find(c => c.id === categoryId)?.color)
    : 'bg-gray-400';
  
  // Внутри категории - распределение по рядам
  const rows = distributeTasksIntoRows(tasks);
  
  categoryGroups.push({
    categoryId,
    categoryName,
    categoryColor,
    tasks,
    rows
  });
});

// Сортировка: категории с tasks, потом "Без категории"
groups.sort((a, b) => {
  if (a.categoryId === null) return 1;  // "Без категории" вниз
  if (b.categoryId === null) return -1;
  return a.categoryName.localeCompare(b.categoryName);
});
```

### 🔧 Технические детали

**Новые состояния:**
```typescript
const [selectedCategories, setSelectedCategories] = React.useState<string[]>([]);
```

**Новый тип данных:**
```typescript
type CategoryGroup = {
  categoryId: string | null;
  categoryName: string;
  categoryColor: string;
  tasks: Task[];
  rows: Task[][];
};
```

**Фильтрация:**
```typescript
// Category filter
if (selectedCategories.length > 0) {
  if (task.categoryId) {
    if (!selectedCategories.includes(task.categoryId)) return false;
  } else {
    if (!selectedCategories.includes('no-category')) return false;
  }
}
```

---

## 📊 Календарь дашборда

### ✨ Обновленный фильтр проектов

Фильтр по проектам теперь работает **как на дашборде** - с правильным разделением личных и проектных задач.

#### Ключевые изменения:

**1. Опция "Личные задачи"**
- 👤 Первая опция в списке
- Отделена линией от проектов
- Иконка пользователя (User)
- ID: `'personal'`

**2. Логика фильтрации**

```typescript
if (selectedProjects.length > 0) {
  if (task.projectId) {
    // Проектная задача - должна быть в выбранных проектах
    if (!selectedProjects.includes(task.projectId)) {
      return false;
    }
  } else {
    // Личная задача - только если выбрано 'personal'
    if (!selectedProjects.includes('personal')) {
      return false;
    }
  }
}
```

**3. Разделение задач**

| Тип задачи | Условие показа |
|------------|----------------|
| **Личная** (`!task.projectId`) | Только если выбрано `'personal'` |
| **Проектная** (`task.projectId`) | Только если выбран этот `projectId` |

**Важно:** Задачи НЕ пересекаются - личная задача не будет показана если выбраны только проекты (и наоборот).

#### Интерфейс фильтра:

```
┌────────────────────────┐
│ Проекты        Все Сброс│
├────────────────────────┤
│ ☑ 👤 Личные задачи    │ ← Первая опция
├────────────────────────┤ ← Разделитель
│ ☑ ● Проект А          │
│ ☐ ● Проект Б          │
│ ☑ ● Проект В          │
└────────────────────────┘
```

### 🎯 Примеры использования

**Сценарий 1: Только личные задачи**
```typescript
selectedProjects = ['personal']
// Показаны: только задачи без projectId
```

**Сценарий 2: Только проектные задачи**
```typescript
selectedProjects = ['project-1', 'project-2']
// Показаны: только задачи с projectId в ['project-1', 'project-2']
// Личные задачи НЕ показаны
```

**Сценарий 3: И личные, и проектные**
```typescript
selectedProjects = ['personal', 'project-1']
// Показаны: личные + задачи из project-1
```

**Сценарий 4: Все (кнопка "Все")**
```typescript
const hasPersonalTasks = tasks.some(t => !t.projectId);
const allIds = projects.map(p => p.id);
if (hasPersonalTasks) allIds.push('personal');
selectedProjects = allIds;
// Показаны: все задачи
```

### 🔧 Код реализации

**Опция "Личные задачи" в UI:**
```tsx
{hasPersonalTasks && (
  <div className="flex items-center gap-2 pb-2 border-b">
    <Checkbox
      id="project-personal"
      checked={selectedProjects.includes('personal')}
      onCheckedChange={() => toggleProject('personal')}
    />
    <label htmlFor="project-personal" className="...">
      <User className="w-3 h-3 text-gray-500" />
      Личные задачи
    </label>
  </div>
)}
```

**Кнопка "Все":**
```typescript
const selectAllProjects = () => {
  const allProjectIds = projects.map(p => p.id);
  const hasPersonalTasks = tasks.some(t => !t.projectId);
  if (hasPersonalTasks) {
    allProjectIds.push('personal');
  }
  setSelectedProjects(allProjectIds);
};
```

---

## 📋 Сводная таблица изменений

| Компонент | Что добавлено | Файл |
|-----------|---------------|------|
| **Календарь проекта** | Структуризация по категориям | `/components/project-calendar-view.tsx` |
| **Календарь проекта** | Фильтр по категориям | `/components/project-calendar-view.tsx` |
| **Календарь проекта** | Группа "Без категории" | `/components/project-calendar-view.tsx` |
| **Календарь дашборда** | Опция "Личные задачи" | `/components/dashboard-calendar-view.tsx` |
| **Календарь дашборда** | Правильная логика фильтрации | `/components/dashboard-calendar-view.tsx` |

---

## 🎨 Визуальное сравнение

### До обновления:

**Календарь проекта:**
```
Все задачи в одной куче
████ Задача 1  ████ Задача 2
    ████ Задача 3
        ████ Задача 4
```

**Календарь дашборда:**
```
Фильтр проектов:
☑ Проект А
☑ Проект Б

Личные задачи тоже показаны (неправильно!)
```

### После обновления:

**Календарь проекта:**
```
● Дизайн (3 задачи)
  ████ Задача 1  ████ Задача 2

● Разработка (1 задача)
      ████ Задача 3

⚪ Без категории (1 задача)
          ████ Задача 4
```

**Календарь дашборда:**
```
Фильтр проектов:
☑ 👤 Личные задачи
├─────────────────
☑ ● Проект А
☐ ● Проект Б

Только выбранные показаны (правильно!)
```

---

## ✅ Тестирование

### Календарь проекта

**Чек-лист:**
1. ✅ Задачи сгруппированы по категориям
2. ✅ Категории отображаются с цветными индикаторами
3. ✅ Есть группа "Без категории" для задач без категории
4. ✅ Счетчики задач корректны
5. ✅ Фильтр по категориям работает
6. ✅ Можно выбрать несколько категорий
7. ✅ Кнопки "Все" и "Сброс" работают
8. ✅ Левая колонка sticky при прокрутке
9. ✅ Высота групп динамическая
10. ✅ Внутри категорий задачи не накладываются

**Тестовые данные:**
```typescript
// Создайте в проекте:
- 3-4 категории (Дизайн, Разработка, Тестирование)
- 10-15 задач с разными категориями
- 2-3 задачи без категории
- Задачи с перекрывающимися датами
```

### Календарь дашборда

**Чек-лист:**
1. ✅ Опция "Личные задачи" первая в списке
2. ✅ "Личные задачи" отделены линией
3. ✅ Личные задачи показаны только если выбрано 'personal'
4. ✅ Проектные задачи не показаны если выбрано только 'personal'
5. ✅ Кнопка "Все" включает личные + все проекты
6. ✅ Счетчик выбранных проектов включает личные
7. ✅ Логика фильтрации работает как на дашборде

**Тестовые данные:**
```typescript
// Создайте:
- 3-4 проекта
- 5-7 задач в проектах
- 3-4 личные задачи (без проекта)
```

**Тестовые сценарии:**

1. **Только личные:**
   - Выберите только "Личные задачи"
   - Должны быть видны только личные задачи

2. **Только проекты:**
   - Выберите 1-2 проекта (без "Личные")
   - Должны быть видны только задачи этих проектов
   - Личные НЕ видны

3. **Смешанный выбор:**
   - Выберите "Личные" + 1 проект
   - Должны быть видны: личные + задачи этого проекта

4. **Все:**
   - Нажмите "Все"
   - Должны быть видны все задачи

---

## 🚀 Следующие шаги

### Возможные улучшения:

1. **Сворачивание категорий**
   - Добавить возможность сворачивать/разворачивать категории
   - Сохранять состояние в localStorage

2. **Drag & Drop между категориями**
   - Возможность перетаскивать задачи между категориями
   - Изменение категории при перетаскивании

3. **Фильтр "Только с задачами"**
   - Скрывать пустые категории
   - Опция в настройках

4. **Экспорт**
   - Экспорт календаря в PDF
   - Экспорт в iCal формат

5. **Масштабирование**
   - Zoom in/out для временной шкалы
   - Разные масштабы (день/неделя/месяц)

---

## 📝 Заключение

Оба календаря получили значительные улучшения:

**Календарь проекта:**
- ✅ Структурирован по категориям
- ✅ Легко фильтровать по категориям
- ✅ Визуально понятная группировка
- ✅ Удобная работа с задачами без категории

**Календарь дашборда:**
- ✅ Правильное разделение личных и проектных задач
- ✅ Соответствие логике дашборда
- ✅ Интуитивный интерфейс фильтра
- ✅ Нет пересечения личных и проектных задач

Эти изменения делают календари более удобными и функциональными для управления задачами в T24!
