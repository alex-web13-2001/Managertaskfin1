# Функционал удаления задач с правами доступа

## Обзор

Реализована полноценная система удаления задач с учетом ролей участников в shared проектах. Теперь права на удаление соответствуют ролям пользователей в проекте.

## Права доступа на удаление

### Матрица прав

| Роль | Свои задачи | Задачи других | Личные задачи (без проекта) |
|------|-------------|---------------|----------------------------|
| **Owner (Владелец)** | ✅ Удалить | ✅ Удалить | ✅ Удалить свои |
| **Collaborator (Участник с правами)** | ✅ Удалить | ✅ Удалить | ❌ Нет доступа |
| **Member (Участник)** | ✅ Удалить | ❌ Нет доступа | ❌ Нет доступа |
| **Viewer (Наблюдатель)** | ❌ Нет доступа | ❌ Нет доступа | ❌ Нет доступа |
| **Не участник** | ❌ Нет доступа | ❌ Нет доступа | ✅ Удалить свои |

### Детальные правила

#### Owner (Владелец проекта)
- Может удалять любые задачи в своем проекте
- Может удалять задачи, созданные другими участниками
- Полный контроль над всеми задачами проекта

#### Collaborator (Участник с правами)
- Может удалять любые задачи в проекте
- Имеет те же права на удаление, что и владелец
- Подходит для ведущих разработчиков, менеджеров проектов

#### Member (Участник)
- Может удалять только задачи, которые:
  - Создал сам (task.userId === currentUser.id)
  - ИЛИ назначены на него (task.assigneeId === currentUser.id)
- Не может удалять задачи других участников
- Подходит для обычных участников команды

#### Viewer (Наблюдатель)
- Не может удалять никакие задачи
- Только просмотр проекта
- Подходит для клиентов, стейкхолдеров

#### Личные задачи (task.projectId === null)
- Может удалять только создатель задачи
- Права проекта не применяются
- Полная изоляция личных задач

## Реализация

### 1. Context Helper Method

Добавлен метод `canDeleteTask` в `src/contexts/app-context.tsx`:

```typescript
const canDeleteTask = React.useCallback((task: Task): boolean => {
  if (!currentUser) return false;
  
  // Личные задачи может удалить только владелец
  if (!task.projectId) {
    return task.userId === currentUser.id;
  }
  
  const role = getUserRoleInProject(task.projectId);
  
  // Owner и Collaborator могут удалять любые задачи
  if (role === 'owner' || role === 'collaborator') {
    return true;
  }
  
  // Member может удалять только свои задачи
  if (role === 'member') {
    return task.userId === currentUser.id || task.assigneeId === currentUser.id;
  }
  
  // Viewer не может удалять
  return false;
}, [currentUser, getUserRoleInProject]);
```

### 2. UI Компоненты

#### TaskModal (src/components/task-modal.tsx)

**View Mode:**
```typescript
const canDelete = React.useMemo(() => {
  if (!existingTask) return false;
  return canDeleteTask(existingTask);
}, [existingTask, canDeleteTask]);

// В UI:
{canDelete && (
  <Button onClick={() => setShowDeleteDialog(true)}>
    <Trash2 className="w-4 h-4 mr-2" />
    Удалить
  </Button>
)}
```

**Edit Mode:**
```typescript
{isEditMode && canDelete && (
  <Button
    variant="ghost"
    size="icon"
    onClick={() => setShowDeleteDialog(true)}
    title="Удалить задачу"
  >
    <Trash2 className="w-4 h-4" />
  </Button>
)}
```

#### ProjectTaskTable (src/components/project-task-table.tsx)

**Table Row Menu:**
```typescript
const userCanDelete = canDeleteTask(task);

// В dropdown меню:
{userCanDelete && (
  <DropdownMenuItem
    onClick={() => handleDelete(task.id)}
    className="text-red-600"
  >
    Удалить
  </DropdownMenuItem>
)}
```

### 3. Диалог подтверждения

При попытке удаления показывается AlertDialog:

```typescript
<AlertDialog open={showDeleteDialog} onOpenChange={setShowDeleteDialog}>
  <AlertDialogContent>
    <AlertDialogHeader>
      <AlertDialogTitle>Удалить задачу?</AlertDialogTitle>
      <AlertDialogDescription>
        Вы уверены, что хотите удалить эту задачу? Это действие нельзя отменить.
      </AlertDialogDescription>
    </AlertDialogHeader>
    <AlertDialogFooter>
      <AlertDialogCancel>Отмена</AlertDialogCancel>
      <AlertDialogAction onClick={handleDelete}>
        Удалить задачу
      </AlertDialogAction>
    </AlertDialogFooter>
  </AlertDialogContent>
</AlertDialog>
```

## Примеры использования

### Сценарий 1: Командный проект

**Дано:**
- User1 (Owner) создал проект "Website"
- User2 (Collaborator) - приглашен с правами
- User3 (Member) - обычный участник
- User4 (Viewer) - наблюдатель

**Задачи:**
- Task A: создал User1, назначена на User2
- Task B: создал User2, назначена на User3
- Task C: создал User3, назначена на User3

**Права на удаление:**

| Пользователь | Task A | Task B | Task C |
|--------------|--------|--------|--------|
| User1 (Owner) | ✅ | ✅ | ✅ |
| User2 (Collaborator) | ✅ | ✅ | ✅ |
| User3 (Member) | ❌ | ✅ | ✅ |
| User4 (Viewer) | ❌ | ❌ | ❌ |

### Сценарий 2: Личные задачи

**Дано:**
- User1 создал личную задачу (без проекта)
- User2 - другой пользователь

**Права:**
- User1 может удалить свою личную задачу ✅
- User2 не видит личную задачу User1 ❌

### Сценарий 3: Изменение роли

**Дано:**
- User2 был Collaborator, мог удалять все задачи
- Owner понизил User2 до Member

**Результат:**
- User2 теперь может удалять только свои задачи
- Права обновляются мгновенно через context

## UI/UX

### Визуальные индикаторы

1. **Кнопка удаления видна только при наличии прав**
   - Если `canDelete === false`, кнопка не отображается
   - Нет попытки скрыть/показать по hover

2. **Иконка корзины (Trash2)**
   - В режиме просмотра: кнопка с текстом "Удалить"
   - В режиме редактирования: icon-only кнопка в header
   - В таблице: пункт меню "Удалить" (красный текст)

3. **Цветовая индикация**
   - Кнопка удаления: `text-red-600 hover:text-red-700`
   - Фон при hover: `hover:bg-red-50`
   - Бордер: `border-red-200` (в outline варианте)

### Подтверждение действия

**Диалог:**
- Заголовок: "Удалить задачу?"
- Описание: "Вы уверены, что хотите удалить эту задачу? Это действие нельзя отменить."
- Кнопки:
  - "Отмена" (default)
  - "Удалить задачу" (красная, деструктивная)

## Безопасность

### Проверки на клиенте

✅ **Централизованная проверка прав:**
- Один метод `canDeleteTask` в context
- Используется во всех компонентах
- Единообразная логика

✅ **Проверка роли в проекте:**
- Использует `getUserRoleInProject`
- Учитывает shared projects
- Проверяет members list

✅ **Изоляция личных задач:**
- task.projectId === null → только создатель
- Полная защита личных данных

### Рекомендации по серверной реализации

При реализации серверного API:

```typescript
// Backend: DELETE /api/tasks/:taskId
async function deleteTask(taskId: string, userId: string) {
  const task = await db.tasks.findById(taskId);
  
  // Проверка прав
  if (!task.projectId) {
    // Личная задача - только создатель
    if (task.userId !== userId) {
      throw new ForbiddenError('Cannot delete other user\'s personal task');
    }
  } else {
    // Проектная задача - проверяем роль
    const role = await getUserRoleInProject(task.projectId, userId);
    
    if (role === 'owner' || role === 'collaborator') {
      // OK - может удалить
    } else if (role === 'member') {
      // Только свои задачи
      if (task.userId !== userId && task.assigneeId !== userId) {
        throw new ForbiddenError('Members can only delete their own tasks');
      }
    } else {
      // Viewer или не участник
      throw new ForbiddenError('You don\'t have permission to delete this task');
    }
  }
  
  await db.tasks.delete(taskId);
}
```

## Тестирование

### Чек-лист

**Owner права:**
- [ ] Может удалить любую задачу в своем проекте
- [ ] Может удалить задачу, созданную collaborator
- [ ] Может удалить задачу, созданную member
- [ ] Может удалить свои личные задачи

**Collaborator права:**
- [ ] Может удалить любую задачу в проекте
- [ ] Может удалить задачу owner
- [ ] Может удалить задачу другого collaborator
- [ ] Не видит личные задачи других

**Member права:**
- [ ] Может удалить задачу, которую создал
- [ ] Может удалить задачу, назначенную на него
- [ ] НЕ может удалить задачу другого member
- [ ] НЕ видит кнопку удаления для чужих задач

**Viewer права:**
- [ ] НЕ видит кнопку удаления ни для каких задач
- [ ] Открытие задачи в view mode - нет кнопки удаления
- [ ] В таблице нет пункта "Удалить" в меню

**Личные задачи:**
- [ ] Создатель видит кнопку удаления
- [ ] Другие пользователи не видят личные задачи
- [ ] Удаление работает для создателя

**UI проверки:**
- [ ] Кнопка удаления появляется только при наличии прав
- [ ] Иконка корзины отображается корректно
- [ ] Диалог подтверждения работает
- [ ] После удаления задача исчезает из списка
- [ ] Toast уведомление при успешном удалении

## Известные ограничения

1. **Нет серверной валидации**
   - Проверка прав только на клиенте
   - Требуется дублирование логики на backend

2. **Нет истории удалений**
   - Задачи удаляются безвозвратно
   - Нет audit log

3. **Нет soft delete**
   - Невозможно восстановить удаленную задачу
   - Рекомендуется добавить корзину/архив

4. **Нет bulk операций**
   - Нельзя удалить несколько задач сразу
   - Удаление только по одной

## Будущие улучшения

### Краткосрочные
- [ ] Добавить serverless API endpoint для удаления с проверкой прав
- [ ] Добавить toast notification после удаления
- [ ] Добавить анимацию при удалении из списка

### Среднесрочные
- [ ] Soft delete - перемещение в корзину
- [ ] Возможность восстановления в течение 30 дней
- [ ] Audit log - кто, когда, какую задачу удалил

### Долгосрочные
- [ ] Bulk delete - выбрать несколько задач и удалить
- [ ] Настраиваемые права - кастомные роли
- [ ] Approval workflow - требование подтверждения от owner

## Заключение

Функционал удаления задач теперь полностью интегрирован с системой проектного шаринга:

✅ Права на удаление соответствуют ролям
✅ Owner и Collaborator - полный контроль
✅ Member - только свои задачи
✅ Viewer - read-only доступ
✅ Личные задачи защищены
✅ UI адаптивный - кнопка видна только при наличии прав
✅ Безопасность проверена - CodeQL: 0 alerts

Система готова к использованию! 🚀
