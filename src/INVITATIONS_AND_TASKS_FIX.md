# Исправление системы приглашений и задач проектов

## Проблема
Приглашенные участники не видели задачи проекта после принятия приглашения.

## Выполненные исправления

### 1. Исправлена логика сохранения задач (Backend)

**До:**
- Все задачи сохранялись с ключом `task:user:${userId}:${taskId}` независимо от того, относятся ли они к проекту

**После:**
- Личные задачи: `task:user:${userId}:${taskId}`
- Задачи проектов: `task:project:${projectId}:${taskId}`

**Файл:** `/supabase/functions/server/index.tsx`

#### Улучшения в создании задач:
```typescript
// Теперь при создании задачи проверяется projectId и выбирается правильный ключ
if (taskData.projectId) {
  // Проверяем доступ к проекту
  storageKey = `task:project:${taskData.projectId}:${taskId}`;
} else {
  storageKey = `task:user:${user.id}:${taskId}`;
}
```

#### Улучшения в обновлении задач:
- Ищем задачу сначала в личных, затем во всех доступных проектах
- Обновляем по правильному ключу

#### Улучшения в удалении задач:
- Ищем и удаляем из правильного места (личные или проектные задачи)

### 2. Улучшено логирование получения задач

Добавлено детальное логирование для отладки:
```
📋 User email@example.com has 5 personal tasks
📁 Total projects in system: 10
  ✅ User is owner of project: My Project (id-123)
  ✅ User is member of project: Shared Project (id-456)
📁 User has access to 2 projects
  📋 Project "My Project" (id-123): 3 tasks
    Task IDs: task-1, task-2, task-3
  📋 Project "Shared Project" (id-456): 7 tasks
    Task IDs: task-4, task-5, ...
✅ Total tasks for user email@example.com: 15 (5 personal + 10 project)
```

### 3. Добавлена утилита миграции задач

Для исправления существующих задач добавлены два новых endpoint:

#### Диагностика: `GET /make-server-d9879966/diagnostics/project/:projectId/tasks`
Показывает:
- Сколько задач в новом формате (`task:project:*`)
- Сколько задач в старом формате (`task:user:*` но с projectId)
- Нужна ли миграция

#### Миграция: `POST /make-server-d9879966/diagnostics/project/:projectId/migrate-tasks`
- Переносит задачи из старого формата в новый
- Удаляет задачи из старого местоположения
- Доступна только владельцу проекта

### 4. Добавлен UI для миграции

**Файл:** `/components/project-about-modal.tsx`

В модальном окне "О проекте" для владельца появился блок:

```
┌─────────────────────────────────────────┐
│ 🔄 Миграция задач                       │
│                                         │
│ Если приглашенные участники не видят    │
│ задачи проекта, нужно выполнить миграцию│
│                                         │
│ [Проверить задачи] [Мигрировать N задач]│
│                                         │
│ ✅ Задач в новом формате: 5             │
│ ⚠️ Задач в старом формате: 3            │
└─────────────────────────────────────────┘
```

### 5. Исправлено отображение приглашений

**Файл:** `/components/header.tsx`

**До:**
- Иконка колокольчика показывалась только при наличии приглашений

**После:**
- Иконка колокольчика показывается всегда
- Бейдж с количеством появляется только при наличии приглашений
- Пустое состояние: "Нет новых приглашений"

## Инструкция по использованию

### Для владельцев проектов

Если у вас уже были созданы задачи в проектах ДО этого обновления:

1. Откройте проект
2. Нажмите на кнопку "О проекте" (иконка Info)
3. Найдите блок "Миграция задач"
4. Нажмите "Проверить задачи"
5. Если система обнаружит задачи в старом формате, появится кнопка "Мигрировать N задач"
6. Нажмите её для переноса задач в новый формат
7. После миграции все участники смогут видеть задачи

### Для новых задач

Все новые задачи автоматически создаются в правильном формате. Миграция не требуется.

### Проверка работы системы

1. **Создайте проект**
2. **Добавьте задачи в проект**
3. **Пригласите участника:**
   - Откройте проект
   - Нажмите "Управление участниками"
   - Добавьте участника по email
4. **Приглашенный участник:**
   - Увидит иконку колокольчика в хедере
   - Примет приглашение
   - Проект появится в списке
   - Задачи проекта будут видны в канбане и таблице

## Технические детали

### Структура хранения задач

```
KV Store:
├── task:user:${userId}:${taskId}          # Личные задачи
├── task:project:${projectId}:${taskId}    # Задачи проектов
└── project:user:${ownerId}:${projectId}   # Проекты
```

### API Endpoints

**Задачи:**
- `GET /make-server-d9879966/tasks` - получить все доступные задачи (личные + из проектов)
- `POST /make-server-d9879966/tasks` - создать задачу (автоопределение типа)
- `PUT /make-server-d9879966/tasks/:id` - обновить задачу
- `DELETE /make-server-d9879966/tasks/:id` - удалить задачу

**Диагностика:**
- `GET /make-server-d9879966/diagnostics/project/:projectId/tasks` - диагностика задач
- `POST /make-server-d9879966/diagnostics/project/:projectId/migrate-tasks` - миграция задач

**Приглашения:**
- `GET /make-server-d9879966/invitations/my` - мои приглашения
- `POST /make-server-d9879966/projects/:id/invitations/:invitationId/accept` - принять приглашение

### Проверка прав доступа

При получении задач проверяется:
1. Пользователь - владелец проекта (`project.userId === user.id`)
2. Пользователь - участник проекта (`project.members` содержит `user.id` или `user.email`)

### Логирование

Все операции с задачами и приглашениями логируются в консоль Supabase Edge Functions для отладки.

## Тестирование

### Сценарий 1: Новый проект
1. ✅ Создать проект
2. ✅ Добавить задачи
3. ✅ Пригласить участника
4. ✅ Участник принимает приглашение
5. ✅ Участник видит проект и задачи

### Сценарий 2: Миграция старых задач
1. ✅ Открыть существующий проект с задачами
2. ✅ Проверить наличие задач в старом формате
3. ✅ Выполнить миграцию
4. ✅ Приглашенные участники видят задачи

### Сценарий 3: Права доступа
1. ✅ Участник может просматривать задачи
2. ✅ Участник с ролью "collaborator" может редактировать
3. ✅ Участник с ролью "viewer" только просматривает
4. ✅ Только владелец может мигрировать задачи

## Что дальше?

Система теперь полностью работает с:
- ✅ Регистрацией и авторизацией
- ✅ Личными задачами
- ✅ Проектами и задачами проектов
- ✅ Приглашениями участников
- ✅ Правами доступа
- ✅ Миграцией данных

Можно продолжить развитие функциональности:
- Уведомления о новых задачах
- Комментарии к задачам
- История изменений
- Расширенные права доступа
