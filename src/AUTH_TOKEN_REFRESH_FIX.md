# Исправление ошибок "Not authenticated" при работе с задачами

## Проблема

Пользователи получали ошибки при обновлении и создании задач:
```
Update task error: Error: Not authenticated
Task save error: Error: Not authenticated
```

## Причины

1. **Отсутствие автоматического обновления токена**
   - Сессия Supabase могла истекать
   - `getAuthToken()` не пыталась обновить истекший токен
   - Не было настройки `autoRefreshToken` в клиенте Supabase

2. **Недостаточная диагностика**
   - Не было логирования состояния токена
   - Неясно, в какой момент терялась аутентификация
   - Пользователь не получал понятного сообщения об ошибке

3. **Отсутствие обработки истечения сессии**
   - При ошибке аутентификации не было автоматического выхода
   - Пользователь оставался в неопределенном состоянии

## Решения

### ✅ 1. Улучшен getAuthToken() с логикой повторной попытки и обновления

**Файл:** `/utils/supabase/client.tsx`

#### До (❌):
```typescript
export const getAuthToken = async () => {
  const { data: { session } } = await supabase.auth.getSession();
  return session?.access_token || null;
};
```

#### После (✅):
```typescript
export const getAuthToken = async (): Promise<string | null> => {
  try {
    // First try to get the current session
    const { data: { session }, error } = await supabase.auth.getSession();
    
    if (error) {
      console.error('❌ getAuthToken: Error getting session:', error);
      return null;
    }
    
    if (session?.access_token) {
      console.log('✅ getAuthToken: Token found');
      return session.access_token;
    }
    
    // If no session, try to refresh
    console.warn('⚠️ getAuthToken: No session found, attempting refresh...');
    const { data: refreshData, error: refreshError } = await supabase.auth.refreshSession();
    
    if (refreshError) {
      console.error('❌ getAuthToken: Error refreshing session:', refreshError);
      return null;
    }
    
    if (refreshData?.session?.access_token) {
      console.log('✅ getAuthToken: Token refreshed successfully');
      return refreshData.session.access_token;
    }
    
    console.error('❌ getAuthToken: No token available after refresh');
    return null;
  } catch (error) {
    console.error('❌ getAuthToken: Unexpected error:', error);
    return null;
  }
};
```

**Улучшения:**
- ✅ Попытка обновить токен если сессии нет
- ✅ Подробное логирование каждого шага
- ✅ Обработка всех возможных ошибок
- ✅ Типизация возвращаемого значения

### ✅ 2. Добавлено автоматическое обновление токенов в Supabase клиенте

**Файл:** `/utils/supabase/client.tsx`

#### До (❌):
```typescript
export const supabase = createSupabaseClient(
  `https://${projectId}.supabase.co`,
  publicAnonKey
);
```

#### После (✅):
```typescript
export const supabase = createSupabaseClient(
  `https://${projectId}.supabase.co`,
  publicAnonKey,
  {
    auth: {
      autoRefreshToken: true,      // ✅ Автоматическое обновление
      persistSession: true,         // ✅ Сохранение сессии
      detectSessionInUrl: true,     // ✅ Определение сессии из URL
    },
  }
);

// Initialize session check
supabase.auth.getSession().then(({ data: { session } }) => {
  if (session) {
    console.log('✅ Supabase: Existing session found');
  } else {
    console.log('ℹ️ Supabase: No existing session');
  }
});
```

**Улучшения:**
- ✅ `autoRefreshToken: true` - токен обновляется автоматически
- ✅ `persistSession: true` - сессия сохраняется между перезагрузками
- ✅ Логирование состояния сессии при инициализации

### ✅ 3. Добавлено подробное логирование в API методы

**Примеры:**

#### tasksAPI.update:
```typescript
update: async (taskId: string, updates: any) => {
  const token = await getAuthToken();
  if (!token) {
    console.error('❌ tasksAPI.update: No auth token available');
    throw new Error('Не авторизован. Пожалуйста, войдите снова.');
  }

  console.log('📝 tasksAPI.update: Updating task', taskId);

  const response = await fetch(`${API_BASE_URL}/tasks/${taskId}`, {
    // ...
  });

  if (!response.ok) {
    const errorText = await response.text();
    console.error('❌ tasksAPI.update: Error response:', errorText);
    // ...
  }

  console.log('✅ tasksAPI.update: Task updated successfully');
  return data.task;
}
```

**Добавлено логирование в:**
- ✅ tasksAPI.getAll
- ✅ tasksAPI.create
- ✅ tasksAPI.update
- ✅ projectsAPI.getAll
- ✅ authAPI.getCurrentUser

### ✅ 4. Улучшена обработка ошибок аутентификации в контексте

**Файл:** `/contexts/app-context.tsx`

```typescript
const createTask = async (taskData: Partial<Task>): Promise<Task> => {
  try {
    const newTask = await tasksAPI.create(taskData);
    setTasks((prev) => [...prev, newTask]);
    toast.success('Задача создана');
    return newTask;
  } catch (error: any) {
    console.error('Create task error:', error);
    
    // ✅ Проверка на ошибку аутентификации
    if (error.message && error.message.includes('авторизован')) {
      toast.error('Сессия истекла. Пожалуйста, войдите снова.', { duration: 5000 });
      // Перезагрузка для возврата на экран входа
      setTimeout(() => {
        window.location.reload();
      }, 2000);
    } else {
      toast.error(error.message || 'Ошибка создания задачи');
    }
    throw error;
  }
};
```

**Улучшения:**
- ✅ Определение ошибок аутентификации
- ✅ Понятное сообщение пользователю на русском языке
- ✅ Автоматический выход и перезагрузка при истечении сессии
- ✅ Добавлено в createTask и updateTask

### ✅ 5. Улучшены сообщения об ошибках

**До:** "Not authenticated" (непонятно для пользователя)
**После:** "Не авторизован. Пожалуйста, войдите снова." (понятное сообщение)

**При истечении сессии:** "Сессия истекла. Пожалуйста, войдите снова."

## Результаты

### ✅ Автоматическое управление токенами
- Токены обновляются автоматически при истечении
- Сессия сохраняется между перезагрузками
- При потере сессии делается попытка обновления

### ✅ Улучшенная диагностика
- Подробное логирование всех операций с токенами
- Легко отследить момент потери аутентификации
- Понятные сообщения об ошибках в консоли

### ✅ Лучший UX
- Пользователь видит понятные сообщения на русском языке
- При истечении сессии происходит автоматический выход
- Нет зависания в неопределенном состоянии

### ✅ Устойчивость к ошибкам
- Обработка всех возможных случаев
- Graceful degradation при ошибках
- Автоматическое восстановление когда возможно

## Тестирование

### Проверить работу токенов:
1. Войти в систему
2. Открыть консоль браузера
3. Создать/обновить задачу
4. Проверить логи: должны быть сообщения типа "✅ getAuthToken: Token found"

### Проверить автоматическое обновление:
1. Войти в систему
2. Оставить страницу открытой на длительное время (>1 часа)
3. Попробовать создать задачу
4. Токен должен обновиться автоматически

### Проверить обработку истечения сессии:
1. Войти в систему
2. Вручную очистить localStorage (или удалить токены в DevTools)
3. Попробовать создать задачу
4. Должно появиться сообщение "Сессия истекла. Пожалуйста, войдите снова."
5. Страница должна перезагрузиться и показать экран входа

## Настройки Supabase Auth

Текущая конфигурация:
```typescript
{
  auth: {
    autoRefreshToken: true,    // Обновлять токен автоматически
    persistSession: true,      // Сохранять сессию
    detectSessionInUrl: true,  // Обнаруживать сессию в URL
  }
}
```

## Известные ограничения

1. **Offline режим**: Если пользователь офлайн, токен не может быть обновлен
2. **Множественные вкладки**: Изменения сессии в одной вкладке не всегда синхронизируются с другими
3. **Время жизни токена**: По умолчанию токен Supabase действителен 1 час

## Дальнейшие улучшения

Возможные будущие улучшения:
- [ ] Добавить retry логику для API запросов
- [ ] Показывать индикатор обновления токена
- [ ] Синхронизировать сессию между вкладками
- [ ] Добавить offline-режим с кэшированием
- [ ] Предупреждать пользователя перед истечением сессии

## Связанные файлы

- `/utils/supabase/client.tsx` - основной API клиент с getAuthToken()
- `/contexts/app-context.tsx` - контекст приложения с обработкой ошибок
- `/App.tsx` - основной компонент с проверкой аутентификации
- `/components/auth-screen.tsx` - экран входа/регистрации

## Ссылки

- [Supabase Auth Documentation](https://supabase.com/docs/guides/auth)
- [Supabase Session Management](https://supabase.com/docs/guides/auth/sessions)
- [Supabase Client Options](https://supabase.com/docs/reference/javascript/initializing)
