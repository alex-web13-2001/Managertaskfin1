# Отладка проблемы "Задачи не появляются в проекте"

## Выполненные исправления

### 1. Синхронизация currentProject с selectedProjectId (App.tsx)

**Проблема:**
Когда пользователь открывал проект через список проектов, переменная `currentProject` (которая используется в TaskModal для установки `initialProject`) не обновлялась автоматически.

**Решение:**
Добавлен useEffect который синхронизирует `currentProject` с `selectedProjectId`:

```typescript
React.useEffect(() => {
  if (selectedProjectId && currentView === 'projects') {
    setCurrentProject(selectedProjectId);
    console.log('🔄 Synced currentProject with selectedProjectId:', selectedProjectId);
  } else if (currentView !== 'projects') {
    setCurrentProject('');
  }
}, [selectedProjectId, currentView]);
```

Теперь когда вы внутри проекта и нажимаете "Новая задача", задача будет создаваться в правильном проекте.

### 2. Добавлено детальное логирование

#### В контексте (contexts/app-context.tsx):
```
✅ Задачи загружены из базы: N
📊 Распределение задач:
  личные: X
  проектные: Y
  поПроектам: { project-id-1: 5, project-id-2: 3 }
📋 Первые 3 задачи: [...]
```

#### В ProjectDetailView:
```
🔍 ProjectDetailView: {
  projectId: '...',
  projectName: '...',
  totalTasks: N,
  tasksForThisProject: M
}
```

#### В ProjectKanbanBoard:
```
🔍 ProjectKanbanBoard filtering: {
  projectId: '...',
  totalTasks: N,
  tasksWithProjectId: M,
  matchingTasks: K,
  taskIds: [...]
}
```

#### В TaskModal при создании:
```
📝 TaskModal - Creating/updating task: {
  mode: 'create',
  taskData: { ... },
  initialProject: '...',
  selectedProjectId: '...',
  isPersonal: false
}
✅ Task created: { ... }
```

#### На сервере (GET /tasks):
```
📋 User email@example.com has X personal tasks
📁 Total projects in system: N
  ✅ User is owner of project: Project Name (id)
  ✅ User is member of project: Another Project (id)
📁 User has access to M projects
  📋 Project "Project Name" (id): K tasks
    Task IDs: task-1, task-2, ...
✅ Total tasks for user: N (X personal + Y project)
```

## Как проверить что исправление работает

### Сценарий 1: Создание задачи из проекта

1. **Откройте проект:**
   - Перейдите в "Проекты"
   - Откройте любой проект

2. **Проверьте логи в консоли:**
   ```
   🔄 Synced currentProject with selectedProjectId: project-xxx-yyy
   🔍 ProjectDetailView: {...}
   ```

3. **Создайте задачу:**
   - Нажмите "Новая задача" в хедере
   - Заполните название
   - Обратите внимание что в выпадающем списке "Проект" уже должен быть выбран текущий проект
   - Нажмите "Создать"

4. **Проверьте логи:**
   ```
   📝 TaskModal - Creating/updating task: {
     projectId: 'project-xxx-yyy',
     ...
   }
   ✅ Task created: {...}
   ```

5. **Проверьте канбан:**
   - Задача должна появиться на канбан-доске проекта в колонке "К выполнению"

### Сценарий 2: Проверка существующих задач

1. **Откройте консоль разработчика (F12)**

2. **Откройте проект:**
   - Перейдите в "Проекты"
   - Откройте проект "Жопааааа" (или любой другой)

3. **Проверьте логи:**
   ```
   🔍 ProjectDetailView: {
     projectId: 'xxx',
     projectName: 'Жопааааа',
     totalTasks: 10,
     tasksForThisProject: 0  <-- Если 0, значит задачи с другим projectId
   }
   
   🔍 ProjectKanbanBoard filtering: {
     projectId: 'xxx',
     totalTasks: 10,
     matchingTasks: 0,
     taskIds: [
       { id: 'task-1', projectId: 'yyy', ... },  <-- Обратите внимание на projectId
       { id: 'task-2', projectId: 'zzz', ... }
     ]
   }
   ```

4. **Если matchingTasks = 0:**
   - Это значит что задачи были созданы в старом формате или с другим projectId
   - Нужно выполнить миграцию (см. следующий раздел)

### Сценарий 3: Миграция старых задач

Если у вас есть задачи которые были созданы ДО исправления и они не появляются в проекте:

1. **Откройте проект**

2. **Нажмите "О проекте"** (кнопка с иконкой Info)

3. **Найдите блок "Миграция задач"** (с желтым фоном)

4. **Нажмите "Проверить задачи"**

5. **Если показано "⚠️ Задач в старом формате: N":**
   - Нажмите "Мигрировать N задач"
   - Дождитесь завершения
   - Задачи должны появиться на канбане

### Сценарий 4: Проверка прав доступа (для приглашенных)

Если вы приглашенный участник и не видите задачи:

1. **Проверьте что приглашение было принято:**
   - Колокольчик в хедере не должен показывать этот проект

2. **Проверьте что проект в списке:**
   - Перейдите в "Проекты"
   - Проект должен быть в списке

3. **Откройте консоль (F12) и проверьте логи сервера:**
   ```
   📁 User has access to N projects
     ✅ User is member of project: Project Name (id)
   📋 Project "Project Name" (id): M tasks
   ```

4. **Если пишет "User has access to 0 projects" или проект не в списке:**
   - Владелец должен заново отправить приглашение
   - Примите приглашение через колокольчик

5. **Если проект есть но задач нет (M = 0):**
   - Владелец должен выполнить миграцию задач (Сценарий 3)

## Частые проблемы и решения

### Проблема: Создал задачу в проекте, но она не появилась

**Решение:**
1. Обновите страницу (F5)
2. Проверьте консоль на наличие ошибок
3. Проверьте что в логах TaskModal передается правильный projectId
4. Если projectId = undefined, значит задача создалась как личная

### Проблема: ProjectId = undefined при создании

**Решение:**
1. Убедитесь что вы внутри проекта (URL должен показывать project detail view)
2. Проверьте что в консоли есть лог "🔄 Synced currentProject with selectedProjectId"
3. Если лога нет - обновите страницу

### Проблема: matchingTasks = 0 хотя в базе есть задачи

**Причины:**
1. Задачи имеют другой projectId
2. Задачи в старом формате (нужна миграция)
3. Фильтры скрывают задачи

**Решение:**
1. Проверьте taskIds в логах - смотрите на projectId каждой задачи
2. Если projectId не совпадает - выполните миграцию
3. Сбросьте фильтры (кнопка "Сбросить" на панели фильтров)

### Проблема: Задачи пропали после миграции

**Это невозможно**, но если произошло:
1. Миграция только копирует и удаляет из старого места
2. Проверьте логи сервера (Supabase Dashboard → Edge Functions → Logs)
3. Найдите логи миграции: "Migrated task X (Title) from old-key to new-key"
4. Если миграция прошла успешно, задачи должны быть в новом формате

## Техническая информация

### Формат ключей в KV Store:

- **Личные задачи:** `task:user:${userId}:${taskId}`
- **Задачи проектов:** `task:project:${projectId}:${taskId}`

### Как задачи фильтруются для проекта:

```typescript
const projectTasks = tasks.filter(task => task.projectId === projectId);
```

Простая проверка - `task.projectId` должен быть равен `projectId` компонента.

### Как задачи создаются:

```typescript
// В TaskModal
const taskData = {
  ...
  projectId: projectId === 'personal' ? undefined : projectId,
};

// На сервере (create task)
if (taskData.projectId) {
  storageKey = `task:project:${taskData.projectId}:${taskId}`;
} else {
  storageKey = `task:user:${user.id}:${taskId}`;
}
```

## Контрольный список для полной проверки

- [ ] Создание личной задачи работает
- [ ] Создание задачи в проекте работает (владелец)
- [ ] Создание задачи в проекте работает (приглашенный участник с правами)
- [ ] Задачи отображаются на канбане проекта
- [ ] Задачи отображаются в дашборде с фильтром по проекту
- [ ] Миграция старых задач работает
- [ ] Приглашенные видят задачи после миграции
- [ ] Логи в консоли показывают правильные данные

Если все пункты выполнены ✅ - система работает корректно!
