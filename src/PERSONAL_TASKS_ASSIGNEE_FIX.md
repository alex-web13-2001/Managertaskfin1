# Автоназначение исполнителя для личных задач

## 📋 Описание изменения

Реализована логика автоматического назначения исполнителя для задач в проекте "Мои задачи" (личные задачи).

## 🎯 Требования

- При создании/редактировании задачи в "Мои задачи" исполнитель автоматически назначается на текущего пользователя
- Поле выбора исполнителя скрыто для личных задач
- Для задач в других проектах поле выбора исполнителя доступно
- Отображается информационное сообщение о том, что задача личная

## ✅ Реализованная логика

### 1. Автоматическое назначение исполнителя

Добавлен `useEffect` в `/components/task-modal.tsx`:

```typescript
// Auto-assign current user for personal tasks
React.useEffect(() => {
  if (projectId === 'personal' && currentUser) {
    // Для "Мои задачи" автоматически назначаем текущего пользователя
    setAssigneeId(currentUser.id);
  } else if (projectId !== 'personal' && !existingTask) {
    // При переключении на другой проект (и это не редактирование существующей задачи),
    // очищаем исполнителя
    setAssigneeId('');
  }
}, [projectId, currentUser, existingTask]);
```

**Поведение:**
- ✅ При выборе "Мои задачи" → автоматически устанавливается `assigneeId = currentUser.id`
- ✅ При переключении с "Мои задачи" на другой проект → `assigneeId` очищается
- ✅ При редактировании существующей задачи → сохраняется текущий исполнитель

### 2. Скрытие поля выбора исполнителя

Модифицирован UI для личных задач:

```typescript
{/* Поле исполнителя скрыто для личных задач */}
{projectId !== 'personal' && (
  <div className="space-y-2">
    <Label>Исполнитель</Label>
    <Select value={assigneeId || 'unassigned'} onValueChange={...}>
      {/* ... */}
    </Select>
  </div>
)}

<div className={`space-y-2 ${projectId === 'personal' ? 'col-span-2' : ''}`}>
  <Label>Дедлайн</Label>
  {/* ... */}
</div>
```

**Поведение:**
- ✅ Для `projectId === 'personal'` поле исполнителя не отображается
- ✅ Поле "Дедлайн" растягивается на всю ширину (`col-span-2`)
- ✅ Для других проектов поле исполнителя отображается нормально

### 3. Информационное сообщение

Добавлено визуальное уведомление для личных задач:

```typescript
{/* Информация для личных задач */}
{projectId === 'personal' && currentUser && (
  <div className="bg-purple-50 border border-purple-200 rounded-lg p-3 flex items-start gap-2">
    <User className="w-4 h-4 text-purple-600 mt-0.5 flex-shrink-0" />
    <div className="text-sm text-purple-900">
      <p className="font-medium">Личная задача</p>
      <p className="text-purple-700 mt-0.5">
        Исполнитель: <span className="font-medium">{currentUser.name}</span> (вы)
      </p>
    </div>
  </div>
)}
```

**Внешний вид:**
- 💜 Фиолетовый фон (`bg-purple-50`)
- 📌 Иконка пользователя
- 📝 Текст: "Личная задача"
- 👤 Информация об исполнителе: "Исполнитель: [Имя пользователя] (вы)"

### 4. Гарантированное сохранение

В функции `handleSubmit` добавлена проверка:

```typescript
// Для личных задач всегда устанавливаем исполнителя как текущего пользователя
const finalAssigneeId = projectId === 'personal' && currentUser 
  ? currentUser.id 
  : (assigneeId || undefined);

const taskData = {
  // ...
  assigneeId: finalAssigneeId,
  // ...
};
```

**Гарантии:**
- ✅ Для личных задач `assigneeId` всегда `currentUser.id`
- ✅ Для других проектов используется выбранный исполнитель или `undefined`

### 5. Сброс формы

Обновлена функция `resetForm`:

```typescript
const resetForm = () => {
  // ...
  const newProjectId = initialProject || 'personal';
  setProjectId(newProjectId);
  // ...
  // Если сброс формы для личных задач, устанавливаем текущего пользователя
  setAssigneeId(newProjectId === 'personal' && currentUser ? currentUser.id : '');
  // ...
};
```

**Поведение:**
- ✅ При сбросе формы для личных задач исполнитель = текущий пользователь
- ✅ При сбросе формы для других проектов исполнитель очищается

## 🧪 Сценарии тестирования

### Сценарий 1: Создание личной задачи
1. Открыть форму создания задачи
2. Убедиться что выбран проект "Мои задачи"
3. **Ожидается:**
   - ✅ Поле "Исполнитель" скрыто
   - ✅ Видно информационное сообщение: "Личная задача. Исполнитель: [Ваше имя] (вы)"
   - ✅ Поле "Дедлайн" занимает всю ширину
4. Создать задачу
5. **Ожидается:**
   - ✅ Задача создана с `assigneeId = currentUser.id`

### Сценарий 2: Создание задачи в проекте
1. Открыть форму создания задачи
2. Выбрать любой проект (не "Мои задачи")
3. **Ожидается:**
   - ✅ Поле "Исполнитель" видно
   - ✅ Информационное сообщение скрыто
   - ✅ Можно выбрать исполнителя из списка
4. Создать задачу с выбранным исполнителем
5. **Ожидается:**
   - ✅ Задача создана с выбранным `assigneeId`

### Сценарий 3: Переключение между проектами
1. Открыть форму создания задачи
2. Выбрать проект (не личный)
3. Выбрать исполнителя
4. Переключиться на "Мои задачи"
5. **Ожидается:**
   - ✅ Поле "Исполнитель" скрылось
   - ✅ Появилось информационное сообщение
   - ✅ `assigneeId` автоматически установлен на текущего пользователя
6. Переключиться обратно на проект
7. **Ожидается:**
   - ✅ Поле "Исполнитель" появилось
   - ✅ `assigneeId` очищен (нужно выбрать заново)

### Сценарий 4: Редактирование личной задачи
1. Открыть существующую личную задачу на редактирование
2. **Ожидается:**
   - ✅ Проект = "Мои задачи"
   - ✅ Поле "Исполнитель" скрыто
   - ✅ Видно информационное сообщение
3. Изменить название задачи
4. Сохранить
5. **Ожидается:**
   - ✅ Задача обновлена
   - ✅ `assigneeId` остался = текущий пользователь

### Сценарий 5: Изменение проекта для существующей задачи
1. Открыть личную задачу на редактирование
2. Сменить проект на другой (не личный)
3. **Ожидается:**
   - ✅ Поле "Исполнитель" появилось
   - ✅ Сохранен предыдущий `assigneeId` (текущий пользователь)
   - ✅ Можно сменить исполнителя
4. Сменить обратно на "Мои задачи"
5. **Ожидается:**
   - ✅ Поле "Исполнитель" скрылось
   - ✅ `assigneeId` = текущий пользователь

## 📁 Измененные файлы

- `/components/task-modal.tsx` - основная логика автоназначения и UI

## 🎨 UI компоненты

### Информационное сообщение (личные задачи)
```
┌────────────────────────────────────────────┐
│ 👤  Личная задача                         │
│     Исполнитель: Иван Иванов (вы)         │
└────────────────────────────────────────────┘
```

### Layout для личных задач
```
┌─────────────────────────────────────────────┐
│ Приоритет      │ Статус                    │
├────────────────────────────────────────────┤
│ Дедлайн (full width)                       │
└────────────────────────────────────────────┘
```

### Layout для проектных задач
```
┌─────────────────────────────────────────────┐
│ Приоритет      │ Статус                    │
├────────────────────────────────────────────┤
│ Исполнитель    │ Дедлайн                   │
└────────────────────────────────────────────┘
```

## 🔧 Технические детали

### Определение личной задачи

Задача считается личной если:
```typescript
projectId === 'personal'
```

### Получение текущего пользователя

```typescript
const { currentUser } = useApp();
```

### Условный рендеринг

```typescript
{projectId !== 'personal' && (
  // Поле исполнителя
)}

{projectId === 'personal' && currentUser && (
  // Информационное сообщение
)}
```

## ⚠️ Важные замечания

1. **Безопасность:** В `handleSubmit` есть финальная проверка, которая гарантирует что для личных задач `assigneeId` всегда будет текущий пользователь, даже если кто-то попытается манипулировать состоянием

2. **Редактирование существующих задач:** При редактировании существующей задачи, если она уже имеет `assigneeId`, он сохраняется до тех пор, пока проект не будет изменен

3. **Переключение проектов:** При переключении с личного проекта на другой, исполнитель очищается только для новых задач. Для редактируемых задач сохраняется текущее значение

## 🎉 Результат

✅ **Личные задачи всегда назначены на текущего пользователя**
- Автоматическое назначение
- Невозможность изменить исполнителя
- Чистый UI без лишних полей

✅ **Проектные задачи работают как раньше**
- Полная свобода выбора исполнителя
- Возможность оставить задачу без исполнителя

✅ **Интуитивный UI**
- Информационное сообщение объясняет поведение
- Адаптивный layout в зависимости от типа задачи

---

**Дата реализации:** 2 ноября 2025  
**Статус:** ✅ Полностью реализовано и протестировано
