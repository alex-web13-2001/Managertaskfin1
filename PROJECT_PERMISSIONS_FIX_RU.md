# Исправление проблем с правами в проектах

## 📋 Описание проблемы

Были обнаружены три критические проблемы с правами доступа в проектах:

1. **Неверная роль при создании проекта**: При создании нового проекта на плашке проекта отображалось "Наблюдатель" вместо "Владелец"
2. **Не работает отправка приглашений**: При нажатии кнопки отправки приглашения ничего не происходило, но пользователю приходило приглашение в "Без названия"
3. **Задачи из проекта не видны**: После принятия приглашения проект появлялся у пользователя, но задачи из проекта не отображались

---

## ✅ Решение

### 1. Исправлена роль владельца проекта

**Файл**: `src/utils/api-client.tsx`

**Проблема**: При создании проекта не устанавливалось поле `userId`, из-за чего система не могла определить владельца проекта.

**Решение**: Добавлено поле `userId` при создании проекта:

```typescript
const newProject = {
  ...projectData,
  id: projectData.id || `project_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`,
  userId: userId, // ✅ Установка владельца проекта
  createdAt: new Date().toISOString(),
  updatedAt: new Date().toISOString(),
};
```

**Результат**: Теперь при создании проекта корректно отображается роль "Владелец".

---

### 2. Исправлена отправка приглашений

**Файл**: `src/utils/api-client.tsx`

**Проблема**: 
- Метод `sendInvitation()` использовал `projectsAPI.getAll()`, который возвращал как собственные, так и общие проекты
- При попытке обновить проект использовался неправильный список проектов
- Название проекта не передавалось корректно

**Решение**: Исправлен метод для прямого получения и обновления проектов владельца:

```typescript
// Получаем проекты владельца напрямую
const ownerProjectsResponse = await fetch(`${API_BASE_URL}/api/kv/projects:${userId}`, {
  headers: {
    'Authorization': `Bearer ${token}`,
  },
});

const ownerProjectsData = await ownerProjectsResponse.json();
const ownerProjects = ownerProjectsData.value || [];
const projectIndex = ownerProjects.findIndex((p: any) => p.id === projectId);

if (projectIndex === -1) {
  throw new Error('Project not found in owner projects');
}

const project = ownerProjects[projectIndex];
projectName = project.name || 'Проект';
project.invitations = [...(project.invitations || []), invitation];
ownerProjects[projectIndex] = project;

// Обновляем список проектов владельца
await fetch(`${API_BASE_URL}/api/kv/projects:${userId}`, {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${token}`,
  },
  body: JSON.stringify({ value: ownerProjects }),
});
```

**Результат**: Приглашения отправляются корректно с правильным названием проекта.

---

### 3. Исправлена видимость задач в общих проектах

**Файл**: `src/utils/api-client.tsx`

**Проблема**: 
- `tasksAPI.getAll()` получал только задачи текущего пользователя
- Задачи из общих проектов не загружались

**Решение**: Расширен метод `getAll()` для загрузки задач из общих проектов:

```typescript
// Получаем задачи пользователя
let allTasks = data.value || [];

// Получаем задачи из общих проектов
const sharedProjectsResponse = await fetch(`${API_BASE_URL}/api/kv/shared_projects:${userId}`, {
  headers: {
    'Authorization': `Bearer ${token}`,
  },
});

if (sharedProjectsResponse.ok) {
  const sharedProjectsData = await sharedProjectsResponse.json();
  const sharedProjects = sharedProjectsData.value || [];
  
  // Для каждого общего проекта получаем задачи от владельца
  for (const projectRef of sharedProjects) {
    const ownerTasksResponse = await fetch(`${API_BASE_URL}/api/kv/tasks:${projectRef.ownerId}`, {
      headers: {
        'Authorization': `Bearer ${token}`,
      },
    });
    
    if (ownerTasksResponse.ok) {
      const ownerTasksData = await ownerTasksResponse.json();
      const ownerTasks = ownerTasksData.value || [];
      
      // Фильтруем задачи, принадлежащие общему проекту
      const projectTasks = ownerTasks.filter((task: any) => task.projectId === projectRef.projectId);
      
      // Добавляем задачи проекта (избегая дубликатов)
      projectTasks.forEach((task: any) => {
        if (!allTasks.find((t: any) => t.id === task.id)) {
          allTasks.push(task);
        }
      });
    }
  }
}
```

**Результат**: Задачи из общих проектов теперь видны всем участникам.

---

### 4. Исправлены операции с задачами в общих проектах

**Файл**: `src/utils/api-client.tsx`

**Проблема**: 
- При создании задачи в общем проекте она добавлялась в список текущего пользователя вместо списка владельца проекта
- При обновлении и удалении задач возникали ошибки

**Решение**: Исправлены методы `create()`, `update()` и `delete()`:

#### Создание задачи:
```typescript
// Определяем, в чей список задач добавлять
let targetUserId = userId;

// Если задача принадлежит проекту, проверяем, общий ли это проект
if (newTask.projectId) {
  const sharedProjectsResponse = await fetch(`${API_BASE_URL}/api/kv/shared_projects:${userId}`, {
    headers: { 'Authorization': `Bearer ${token}` },
  });
  
  if (sharedProjectsResponse.ok) {
    const sharedProjectsData = await sharedProjectsResponse.json();
    const sharedProjects = sharedProjectsData.value || [];
    const sharedProject = sharedProjects.find((ref: any) => ref.projectId === newTask.projectId);
    
    if (sharedProject) {
      // Это общий проект, добавляем задачу в список владельца
      targetUserId = sharedProject.ownerId;
    }
  }
}

// Сохраняем в список целевого пользователя
await fetch(`${API_BASE_URL}/api/kv/tasks:${targetUserId}`, {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${token}`,
  },
  body: JSON.stringify({ value: tasks }),
});
```

Аналогично исправлены методы `update()` и `delete()`.

**Результат**: Задачи в общих проектах создаются, обновляются и удаляются корректно.

---

### 5. Исправлено обновление проектов

**Файл**: `src/utils/api-client.tsx`

**Проблема**: Метод `update()` использовал `getAll()`, что могло привести к обновлению не того проекта.

**Решение**: Исправлен метод для работы только с проектами владельца:

```typescript
// Получаем только проекты владельца, не общие проекты
const ownerProjectsResponse = await fetch(`${API_BASE_URL}/api/kv/projects:${userId}`, {
  headers: {
    'Authorization': `Bearer ${token}`,
  },
});

const ownerProjectsData = await ownerProjectsResponse.json();
const projects = ownerProjectsData.value || [];
const index = projects.findIndex((p: any) => p.id === projectId);

if (index === -1) {
  throw new Error('Project not found or you do not have permission to update it');
}

// Обновляем проект
projects[index] = {
  ...projects[index],
  ...updates,
  updatedAt: new Date().toISOString(),
};

// Сохраняем в список владельца
await fetch(`${API_BASE_URL}/api/kv/projects:${userId}`, {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${token}`,
  },
  body: JSON.stringify({ value: projects }),
});

// Также обновляем общий доступ
await fetch(`${API_BASE_URL}/api/kv/project:${projectId}`, {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${token}`,
  },
  body: JSON.stringify({ value: projects[index] }),
});
```

---

## 🎯 Результаты

После исправлений:

✅ **При создании проекта** корректно отображается роль "Владелец"  
✅ **При отправке приглашения** оно создается с правильным названием проекта  
✅ **После принятия приглашения** пользователь видит все задачи проекта  
✅ **Операции с задачами** работают корректно для всех участников проекта  
✅ **Обновление проектов** работает безопасно и корректно  

---

## 🔒 Безопасность

Проведена проверка безопасности с помощью CodeQL:
- **Результат**: Уязвимостей не обнаружено ✅

---

## 📝 Затронутые файлы

1. `src/utils/api-client.tsx` - Основной файл с исправлениями

---

## 🧪 Тестирование

Для тестирования исправлений:

1. **Создать новый проект**
   - Убедиться, что роль отображается как "Владелец"

2. **Отправить приглашение**
   - Отправить приглашение другому пользователю
   - Проверить, что приглашение содержит правильное название проекта

3. **Принять приглашение**
   - Войти под другим пользователем
   - Принять приглашение
   - Убедиться, что проект появился в списке
   - Убедиться, что задачи проекта видны

4. **Создать задачу в общем проекте**
   - Создать задачу от имени участника
   - Убедиться, что задача видна владельцу
   - Проверить возможность обновления и удаления

---

## 📚 Дополнительная документация

- [PROJECT_SHARING_GUIDE_RU.md](./PROJECT_SHARING_GUIDE_RU.md) - Руководство по общему доступу к проектам
- [EMAIL_INVITE_PERMISSIONS_GUIDE_RU.md](./EMAIL_INVITE_PERMISSIONS_GUIDE_RU.md) - Руководство по правам доступа при приглашении
- [TASK_DELETION_PERMISSIONS_RU.md](./TASK_DELETION_PERMISSIONS_RU.md) - Руководство по правам удаления задач
