# Real-time обновления в 24Task

## ✅ Что сделано

Добавлена система real-time синхронизации через Supabase Realtime, которая позволяет всем участникам команды видеть изменения задач и проектов мгновенно, без обновления страницы.

## 🔴 Как это работает

### Подписки на изменения

Приложение автоматически подключается к двум каналам real-time обновлений:

1. **tasks-realtime** - отслеживает все изменения в таблице задач
2. **projects-realtime** - отслеживает все изменения в таблице проектов

### Типы событий

Система отслеживает 3 типа событий:

- **INSERT** - создание новой задачи/проекта
  - Автоматически добавляет новую запись в state
  - Показывает уведомление с названием
  
- **UPDATE** - обновление существующей задачи/проекта
  - Автоматически обновляет запись в state
  - Все открытые компоненты сразу видят изменения
  
- **DELETE** - удаление задачи/проекта
  - Автоматически удаляет запись из state
  - Показывает уведомление об удалении

## 📊 Преимущества

✅ **Мгновенная синхронизация** - все участники видят изменения в реальном времени  
✅ **Без миграций** - использует стандартные возможности Supabase  
✅ **Автоматическое обновление UI** - React state обновляется автоматически  
✅ **Предотвращение дубликатов** - проверка перед добавлением новых записей  
✅ **Умные уведомления** - показываются только для важных событий  

## 🎯 Примеры использования

### Сценарий 1: Создание задачи
1. Пользователь А создает задачу в проекте
2. Real-time событие INSERT отправляется всем подписчикам
3. Пользователь Б мгновенно видит новую задачу на канбан-доске
4. Уведомление: "Добавлена новая задача: [название]"

### Сценарий 2: Перемещение задачи
1. Пользователь А перетаскивает задачу в другую колонку
2. Real-time событие UPDATE отправляется
3. У всех участников задача автоматически перемещается в новую колонку
4. Никаких уведомлений (чтобы не спамить)

### Сценарий 3: Удаление проекта
1. Пользователь А удаляет проект
2. Real-time событие DELETE отправляется
3. У всех участников проект исчезает из списка
4. Уведомление: "Проект удален"

## 🔧 Техническая реализация

### Файлы изменены

- `/contexts/app-context.tsx` - добавлены real-time подписки и состояние подключения
- `/components/realtime-indicator.tsx` - визуальный индикатор статуса подключения
- `/components/header.tsx` - отображение индикатора в шапке приложения
- `/utils/supabase/client.tsx` - экспортирован Supabase client

### Код подписки

```typescript
const tasksChannel = supabase
  .channel('tasks-realtime')
  .on('postgres_changes', {
    event: '*',
    schema: 'public',
    table: 'tasks_d9879966',
  }, (payload) => {
    // Обработка событий INSERT/UPDATE/DELETE
  })
  .subscribe();
```

### Предотвращение дубликатов

При получении события INSERT проверяется, нет ли уже задачи с таким ID:

```typescript
setTasks((prev) => {
  if (prev.some(t => t.id === newTask.id)) {
    return prev; // Уже есть
  }
  return [...prev, newTask]; // Добавляем
});
```

## 📝 Логирование

Все события логируются в консоль:
- 🔴 `Real-time событие для задач: INSERT`
- ➕ `Новая задача создана: [название]`
- 📝 `Задача обновлена: [название]`
- 🗑️ `Задача удалена: [id]`

## ⚡ Производительность

- Подписки создаются один раз при монтировании AppProvider
- Автоматическая очистка при размонтировании (cleanup)
- Минимальная нагрузка на клиента
- Нет дополнительных API запросов

## 👁️ Визуальный индикатор

В правом верхнем углу header (на десктопе) теперь отображается индикатор статуса подключения:

- 🟢 **Online** с пульсирующей точкой - real-time подключено
- ⚫ **Offline** - подключение не установлено

Это позволяет пользователям всегда видеть, работает ли синхронизация в реальном времени.

## 🎉 Результат

Теперь 24Task - это полноценный collaborative инструмент с мгновенной синхронизацией, как в современных продуктах типа Trello, Asana или Notion!

## 🧪 Как протестировать

1. Откройте приложение в двух разных вкладках браузера
2. В первой вкладке создайте новую задачу
3. Во второй вкладке задача появится автоматически через 1-2 секунды
4. Попробуйте переместить задачу на канбан-доске в одной вкладке
5. Во второй вкладке задача переместится автоматически
6. Проверьте индикатор "Online" в header - он должен быть зеленым
