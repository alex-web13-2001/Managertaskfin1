# Исправление: Общие категории проекта для всех участников

## Проблема

Участники проекта не видели категории, которые владелец или участник с правами прикрепил к проекту через поле `availableCategories`.

### Причина

Категории хранятся в формате `category:user:{userId}:{categoryId}`, что означает:
- Каждая категория принадлежит конкретному пользователю
- GET `/categories` возвращал только `category:user:${currentUserId}:*`
- Когда владелец прикреплял свою категорию к проекту, участники её не видели, так как она хранилась как `category:user:${ownerId}:{categoryId}`

### Пример проблемы

1. **Владелец** создаёт категорию "Дизайн" → `category:user:owner-id:cat-1`
2. **Владелец** прикрепляет категорию к проекту → `project.availableCategories = ['cat-1']`
3. **Участник** открывает задачу в проекте → GET `/categories` возвращает только `category:user:member-id:*`
4. **Результат**: Участник не видит категорию "Дизайн"

## Решение

Изменена логика загрузки категорий в `GET /make-server-d9879966/categories`:

### Новый алгоритм

1. **Загрузить собственные категории пользователя**
   ```typescript
   const ownCategories = await kv.getByPrefix(`category:user:${user.id}:`)
   ```

2. **Получить все проекты пользователя** (владелец + участник)
   ```typescript
   const ownProjects = await kv.getByPrefix(`project:user:${user.id}:`)
   const sharedProjects = /* проекты где пользователь участник */
   const allProjects = [...ownProjects, ...sharedProjects]
   ```

3. **Собрать ID категорий из всех проектов**
   ```typescript
   const projectCategoryIds = new Set<string>()
   for (const project of allProjects) {
     if (project.availableCategories) {
       project.availableCategories.forEach(catId => projectCategoryIds.add(catId))
     }
   }
   ```

4. **Загрузить категории из проектов** (независимо от владельца)
   ```typescript
   const allCategories = await kv.getByPrefix(`category:user:`)
   const projectCategories = allCategories.filter(cat => 
     projectCategoryIds.has(cat.id)
   )
   ```

5. **Объединить и дедуплицировать**
   ```typescript
   const categoriesMap = new Map()
   ownCategories.forEach(cat => categoriesMap.set(cat.id, cat))
   projectCategories.forEach(cat => {
     if (!categoriesMap.has(cat.id)) {
       categoriesMap.set(cat.id, cat)
     }
   })
   const allUserCategories = Array.from(categoriesMap.values())
   ```

## Изменённые файлы

### `/supabase/functions/server/index.tsx`

**Endpoint:** `GET /make-server-d9879966/categories`

**Было:**
```typescript
const categories = await kv.getByPrefix(`category:user:${user.id}:`) || [];
return c.json({ categories });
```

**Стало:**
```typescript
// 1. Загружаем собственные категории
const ownCategories = await kv.getByPrefix(`category:user:${user.id}:`) || [];

// 2. Получаем все проекты пользователя
const allProjects = [...ownProjects, ...sharedProjects];

// 3. Собираем ID категорий из проектов
const projectCategoryIds = new Set<string>();
for (const project of allProjects) {
  if (project.availableCategories) {
    project.availableCategories.forEach(catId => projectCategoryIds.add(catId));
  }
}

// 4. Загружаем категории из проектов
const allCategories = await kv.getByPrefix(`category:user:`) || [];
const projectCategories = allCategories.filter(cat => projectCategoryIds.has(cat.id));

// 5. Объединяем и дедуплицируем
const categoriesMap = new Map();
ownCategories.forEach(cat => categoriesMap.set(cat.id, cat));
projectCategories.forEach(cat => {
  if (!categoriesMap.has(cat.id)) {
    categoriesMap.set(cat.id, cat);
  }
});

const allUserCategories = Array.from(categoriesMap.values());
return c.json({ categories: allUserCategories });
```

## Как это работает теперь

### Сценарий 1: Владелец прикрепляет категорию к проекту

1. **Владелец** создаёт категорию "Дизайн" → сохраняется в KV как `category:user:owner-id:design-id`
2. **Владелец** редактирует проект и прикрепляет категорию → `project.availableCategories = ['design-id']`
3. **Участник** открывает приложение → GET `/categories`:
   - Загружаются собственные категории участника
   - Загружаются все проекты участника (включая этот проект)
   - Находятся ID категорий из `availableCategories` всех проектов
   - Загружается категория "Дизайн" из KV (по ID, независимо от владельца)
   - Категория "Дизайн" добавляется в список доступных категорий
4. **Участник** создаёт задачу в проекте → видит категорию "Дизайн" в списке

### Сценарий 2: Участник с правами прикрепляет свою категорию

1. **Участник с правами** создаёт категорию "Тестирование" → `category:user:collaborator-id:testing-id`
2. **Участник с правами** редактирует проект → добавляет в `availableCategories`
3. **Все участники проекта** видят категорию "Тестирование" при создании задач

### Сценарий 3: Множественные проекты

1. Пользователь участвует в 3 проектах:
   - Проект A (владелец: User1, категории: [cat-1, cat-2])
   - Проект B (владелец: User2, категории: [cat-3])
   - Проект C (владелец: текущий пользователь, категории: [cat-4, cat-5])

2. GET `/categories` вернёт:
   - Собственные категории пользователя: cat-4, cat-5
   - Категории из проекта A: cat-1, cat-2
   - Категории из проекта B: cat-3
   - **Итого: 5 категорий**

## Преимущества

✅ **Общие категории**: Владелец и участники с правами могут создавать категории и делать их доступными всем участникам проекта

✅ **Автоматическая синхронизация**: Как только категория прикреплена к проекту, все участники автоматически её видят

✅ **Нет дублирования**: Одна и та же категория может использоваться в нескольких проектах без создания копий

✅ **Сохранение приватности**: Категории, не прикреплённые к проектам, остаются приватными для владельца

✅ **Обратная совместимость**: Все существующие категории продолжают работать

## Логи для отладки

При загрузке категорий в консоли будут следующие логи:

```
✅ Found 3 own categories for user user-123
📦 User participates in 5 projects
🏷️ Found 7 unique category IDs in projects
📥 Loaded 4 project categories
✅ Total 7 categories available for user user-123
```

## Проверка

### Тест 1: Владелец создаёт категорию и прикрепляет к проекту

1. Авторизуйтесь как владелец проекта
2. Перейдите в "Справочники" → создайте категорию "Новая функция"
3. Откройте проект → нажмите "⋮" → "Редактировать"
4. В поле "Доступные категории" выберите "Новая функция"
5. Авторизуйтесь как участник проекта
6. Создайте задачу в этом проекте
7. **Ожидается**: В списке категорий есть "Новая функция"

### Тест 2: Участник с правами создаёт и прикрепляет категорию

1. Авторизуйтесь как участник с правами (collaborator)
2. Создайте категорию "Ревью кода"
3. Откройте проект → отредактируйте → прикрепите категорию
4. Авторизуйтесь как другой участник (member или viewer)
5. **Ожидается**: Категория "Ревью кода" видна при создании задачи

### Тест 3: Множественные проекты

1. Создайте 2 проекта с разными владельцами
2. Прикрепите к каждому проекту разные категории
3. Пригласите одного пользователя в оба проекта
4. **Ожидается**: Пользователь видит категории из обоих проектов + свои собственные

## Потенциальные проблемы и решения

### Проблема: Слишком много категорий загружается

**Решение**: В будущем можно добавить пагинацию или фильтрацию категорий. Сейчас загружаются только категории из проектов пользователя, что ограничивает количество.

### Проблема: Категория удалена владельцем, но всё ещё в availableCategories

**Решение**: При удалении категории нужно также удалить её из `availableCategories` всех проектов. Это можно добавить в будущем.

### Проблема: Конфликт имён категорий

**Решение**: Категории идентифицируются по ID, а не по имени. Разные пользователи могут создать категории с одинаковыми именами, но они будут иметь разные ID.

## Следующие шаги (опционально)

- [ ] Добавить UI для управления категориями проекта в модальном окне редактирования проекта
- [ ] Показывать владельца категории в списке (если категория не своя)
- [ ] Автоматически удалять категорию из проектов при её удалении
- [ ] Добавить возможность "клонировать" категорию из проекта в свои категории
