# T24 - Современный таск-менеджер

<div align="center">
  <h3>🚀 Интерактивный прототип системы управления задачами и проектами</h3>
  <p>Полнофункциональное веб-приложение с Kanban-досками, табличным видом и системой прав доступа</p>
</div>

---

## 📋 Содержание

- [О проекте](#о-проекте)
- [Возможности](#возможности)
- [Технологический стек](#технологический-стек)
- [Архитектура](#архитектура)
- [Установка и запуск](#установка-и-запуск)
- [Настройка Supabase](#настройка-supabase)
- [Структура проекта](#структура-проекта)
- [API документация](#api-документация)
- [Система прав доступа](#система-прав-доступа)
- [Разработка](#разработка)
- [Известные проблемы](#известные-проблемы)
- [Лицензия](#лицензия)

---

## 🎯 О проекте

**T24** — это современный таск-менеджер, созданный с использованием React, TypeScript и Supabase. Проект демонстрирует полный цикл разработки SaaS-приложения от аутентификации до сложной системы управления проектами с различными ролями участников.

### Ключевые особенности:

- ✅ **Полная аутентификация** - регистрация, вход, управление сессиями
- 📊 **Два режима отображения** - Kanban доски и табличный вид
- 👥 **Система ролей** - владелец, участник с правами, участник, наблюдатель
- 🎨 **Кастомные статусы** - настройка колонок Kanban для каждого проекта
- 📎 **Вложения** - загрузка файлов к задачам через Supabase Storage
- 🔔 **Realtime обновления** - автоматическая синхронизация данных
- 📱 **Адаптивный дизайн** - работает на всех устройствах
- 🌈 **Современный UI** - цветовая схема (фиолетовый, зелёный, оранжевый, розовый)

---

## 🚀 Возможности

### Управление задачами
- Создание, редактирование и удаление задач
- Назначение исполнителей
- Установка приоритетов (низкий, средний, высокий, критический)
- Прикрепление файлов
- Комментарии к задачам
- Фильтрация и поиск

### Управление проектами
- Создание проектов с описанием
- Приглашение участников по email
- Настройка кастомных колонок Kanban
- Управление правами доступа
- Архивирование проектов

### Личные задачи
- Отдельный раздел для персональных задач
- Kanban доска и табличный вид
- Все возможности обычных задач

### Профиль пользователя
- Редактирование имени и email
- Загрузка аватара
- Управление паролем

### Справочники
- Категории задач
- Настройка цветов и иконок

---

## 🛠 Технологический стек

### Frontend
- **React 18** - библиотека для построения UI
- **TypeScript** - типизация
- **Tailwind CSS v4** - стилизация
- **Lucide React** - иконки
- **React DnD** - drag & drop для Kanban
- **Sonner** - toast уведомления
- **Motion** - анимации

### Backend
- **Supabase** - Backend as a Service
  - PostgreSQL - база данных
  - Edge Functions - serverless API
  - Auth - аутентификация
  - Storage - хранение файлов
  - Realtime - live обновления
- **Hono** - веб-фреймворк для Edge Functions
- **Deno** - runtime для serverless функций

### Инструменты разработки
- **Vite** - сборщик (используется Figma Make)
- **ESLint** - линтинг
- **Git** - контроль версий

---

## 🏗 Архитектура

Проект использует трехуровневую архитектуру:

```
┌─────────────────────────────────────────┐
│           Frontend (React)              │
│  - UI Components                        │
│  - App Context (глобальное состояние)  │
│  - Supabase Client                      │
└──────────────┬──────────────────────────┘
               │
               │ HTTPS Requests
               │ Authorization: Bearer <token>
               │
┌──────────────▼──────────────────────────┐
│    Server (Supabase Edge Function)      │
│  - Hono Web Server                      │
│  - API Routes                           │
│  - Auth Middleware                      │
│  - Business Logic                       │
└──────────────┬──────────────────────────┘
               │
               │ SQL Queries
               │
┌──────────────▼──────────────────────────┐
│      Database (PostgreSQL)              │
│  - kv_store table (key-value)           │
│  - Storage buckets                      │
│  - Auth tables                          │
└─────────────────────────────────────────┘
```

### Основные компоненты

#### App Context (`/contexts/app-context.tsx`)
Глобальное состояние приложения:
- Управление пользователем и сессией
- Загрузка и кэширование задач, проектов, категорий
- Polling система для автоматических обновлений (каждые 3 секунды)
- Centralised API calls

#### Supabase Client (`/utils/supabase/client.tsx`)
Singleton клиент для взаимодействия с Supabase Auth

#### Server (`/supabase/functions/server/index.tsx`)
Hono веб-сервер с маршрутами для всех операций

---

## 📦 Установка и запуск

### Предварительные требования

- Node.js 18+ 
- npm или yarn
- Аккаунт Supabase (бесплатный план подойдет)
- Supabase CLI (для деплоя Edge Functions)

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/yourusername/t24-task-manager.git
cd t24-task-manager
```

### Шаг 2: Установка зависимостей

```bash
npm install
```

### Шаг 3: Настройка переменных окружения

Проект работает на платформе Figma Make, которая автоматически управляет следующими переменными:

```bash
SUPABASE_URL=your_supabase_url
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
SUPABASE_DB_URL=your_database_url
```

Если вы запускаете проект вне Figma Make, создайте файл `.env.local`:

```bash
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

### Шаг 4: Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

---

## ⚙️ Настройка Supabase

### 1. Создание проекта

1. Зайдите на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Дождитесь инициализации базы данных

### 2. Настройка базы данных

Выполните SQL запросы в SQL Editor:

```sql
-- Создание таблицы key-value store
CREATE TABLE IF NOT EXISTS kv_store_d9879966 (
  key TEXT PRIMARY KEY,
  value JSONB NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Индексы для быстрого поиска
CREATE INDEX IF NOT EXISTS idx_kv_store_key_prefix 
ON kv_store_d9879966 (key text_pattern_ops);

-- Row Level Security (RLS)
ALTER TABLE kv_store_d9879966 ENABLE ROW LEVEL SECURITY;

-- Политика: Service role может делать всё
CREATE POLICY "Service role has full access" 
ON kv_store_d9879966 
FOR ALL 
USING (true) 
WITH CHECK (true);
```

### 3. Настройка Storage

1. Перейдите в раздел Storage
2. Бакеты будут созданы автоматически при первом запуске сервера:
   - `make-d9879966-task-attachments` - для вложений задач
   - `make-d9879966-avatars` - для аватаров пользователей

### 4. Настройка Auth

1. Перейдите в Authentication → Settings
2. Включите Email provider
3. Отключите email confirmation (для прототипа):
   - Settings → Auth → Email Auth → Disable "Confirm email"
4. Настройте Site URL: `http://localhost:5173`

### 5. Деплой Edge Functions

```bash
# Установка Supabase CLI
npm install -g supabase

# Логин
supabase login

# Связать проект
supabase link --project-ref your-project-ref

# Деплой функций
supabase functions deploy server
```

### 6. Настройка переменных окружения для Edge Function

```bash
supabase secrets set SUPABASE_URL=https://your-project.supabase.co
supabase secrets set SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
```

---

## 📁 Структура проекта

```
t24-task-manager/
├── components/              # React компоненты
│   ├── ui/                 # Базовые UI компоненты
│   ├── auth-screen.tsx     # Экран входа/регистрации
│   ├── dashboard-view.tsx  # Главная панель
│   ├── tasks-view.tsx      # Все задачи
│   ├── projects-view.tsx   # Все проекты
│   ├── project-detail-view.tsx  # Детали проекта
│   ├── personal-*.tsx      # Личные задачи
│   ├── categories-view.tsx # Справочник категорий
│   ├── profile-view.tsx    # Профиль пользователя
│   ├── archive-view.tsx    # Архив
│   ├── kanban-board.tsx    # Kanban доска
│   ├── task-table.tsx      # Табличный вид
│   ├── task-modal.tsx      # Создание/редактирование задачи
│   ├── task-detail-modal.tsx  # Просмотр задачи
│   └── ...
├── contexts/
│   └── app-context.tsx     # Глобальное состояние
├── supabase/
│   └── functions/
│       └── server/
│           ├── index.tsx   # Главный файл сервера
│           ├── kv_store.tsx # Утилиты для KV store
│           └── demo-data.tsx # Демо данные
├── utils/
│   ├── supabase/
│   │   ├── client.tsx      # Supabase клиент
│   │   └── info.tsx        # Конфигурация
│   └── dev-tools-config.tsx # React DevTools
├── styles/
│   └── globals.css         # Глобальные стили
├── App.tsx                 # Главный компонент
└── README.md              # Документация
```

---

## 📡 API документация

Все API endpoints находятся по адресу:
```
https://{projectId}.supabase.co/functions/v1/make-server-d9879966/{route}
```

### Authentication

#### POST `/signup`
Регистрация нового пользователя

**Body:**
```json
{
  "email": "user@example.com",
  "password": "password123",
  "name": "John Doe"
}
```

**Response:**
```json
{
  "success": true,
  "user": {
    "id": "uuid",
    "email": "user@example.com"
  },
  "access_token": "jwt_token"
}
```

### Tasks

#### GET `/tasks`
Получить все задачи пользователя

**Headers:** `Authorization: Bearer {access_token}`

**Response:**
```json
{
  "success": true,
  "tasks": [
    {
      "id": "task_uuid",
      "title": "Название задачи",
      "description": "Описание",
      "status": "в работе",
      "priority": "высокий",
      "project_id": "project_uuid",
      "assignee_id": "user_uuid",
      "created_by": "user_uuid",
      "due_date": "2025-11-15",
      "category_id": "category_uuid",
      "attachments": [],
      "comments": []
    }
  ]
}
```

#### POST `/tasks`
Создать новую задачу

**Headers:** `Authorization: Bearer {access_token}`

**Body:**
```json
{
  "title": "Новая задача",
  "description": "Описание задачи",
  "status": "к выполнению",
  "priority": "средний",
  "project_id": "project_uuid",
  "assignee_id": "user_uuid",
  "due_date": "2025-11-15",
  "category_id": "category_uuid"
}
```

#### PUT `/tasks/:id`
Обновить задачу

#### DELETE `/tasks/:id`
Удалить задачу

### Projects

#### GET `/projects`
Получить все проекты пользователя

#### POST `/projects`
Создать проект

**Body:**
```json
{
  "name": "Новый проект",
  "description": "Описание проекта",
  "color": "purple"
}
```

#### PUT `/projects/:id`
Обновить проект

#### DELETE `/projects/:id`
Удалить проект

### Custom Columns

#### GET `/custom-columns/:projectId`
Получить кастомные колонки проекта

#### POST `/custom-columns/:projectId`
Создать/обновить кастомные колонки

**Body:**
```json
{
  "columns": [
    {
      "id": "backlog",
      "name": "Бэклог",
      "color": "#9333ea"
    },
    {
      "id": "in_progress",
      "name": "В работе",
      "color": "#3b82f6"
    }
  ]
}
```

### Invitations

#### POST `/invitations`
Пригласить участника в проект

**Body:**
```json
{
  "project_id": "project_uuid",
  "email": "member@example.com",
  "role": "member"
}
```

#### GET `/invitations`
Получить приглашения пользователя

#### POST `/invitations/:id/accept`
Принять приглашение

#### POST `/invitations/:id/decline`
Отклонить приглашение

### File Upload

#### POST `/upload-attachment`
Загрузить вложение к задаче

**Headers:** 
- `Authorization: Bearer {access_token}`
- `Content-Type: multipart/form-data`

**Form Data:**
- `file`: File
- `taskId`: string

#### POST `/upload-avatar`
Загрузить аватар пользователя

---

## 🔐 Система прав доступа

### Роли участников проекта

| Роль | Код | Права |
|------|-----|-------|
| **Владелец** | `owner` | Полный доступ: управление проектом, участниками, задачами. Может удалить проект |
| **Участник с правами** | `collaborator` | Может видеть и редактировать все задачи, редактировать проект, но не может удалить его |
| **Участник** | `member` | Видит только назначенные на него задачи. Может создавать и редактировать свои задачи. Не может редактировать проект |
| **Наблюдатель** | `viewer` | Может только просматривать задачи. Не может ничего редактировать |

### Матрица прав

|  | Владелец | Участник с правами | Участник | Наблюдатель |
|---|:---:|:---:|:---:|:---:|
| Просмотр всех задач | ✅ | ✅ | ❌* | ✅ |
| Создание задач | ✅ | ✅ | ✅ | ❌ |
| Редактирование всех задач | ✅ | ✅ | ❌* | ❌ |
| Удаление задач | ✅ | ✅ | ❌* | ❌ |
| Редактирование проекта | ✅ | ✅ | ❌ | ❌ |
| Удаление проекта | ✅ | ❌ | ❌ | ❌ |
| Управление участниками | ✅ | ❌ | ❌ | ❌ |
| Настройка колонок Kanban | ✅ | ✅ | ❌ | ❌ |

*Участник может редактировать только назначенные на него задачи

---

## 👨‍💻 Разработка

### Демо данные

При первом входе автоматически создаются демо данные:
- 3 категории задач (Разработка, Дизайн, Маркетинг)
- 2 проекта (Редизайн сайта, Мобильное приложение)
- Несколько задач с разными статусами

Демо данные создаются в `/supabase/functions/server/demo-data.tsx`

### Polling система

Приложение использует polling для автоматических обновлений:
- Задачи обновляются каждые 3 секунды
- Проекты обновляются каждые 3 секунды
- Кастомные колонки обновляются каждые 3 секунды

Настройка в `app-context.tsx`

### Добавление новых компонентов

1. Создайте компонент в `/components/`
2. Импортируйте в `App.tsx` или родительский компонент
3. Используйте `AppContext` для доступа к глобальному состоянию

```tsx
import { useApp } from './contexts/app-context';

function MyComponent() {
  const { tasks, projects, createTask } = useApp();
  
  // Ваш код
}
```

### Стилизация

Проект использует Tailwind CSS v4 с кастомными токенами в `styles/globals.css`:

```css
@theme {
  --color-primary: #9333ea;
  --color-success: #10b981;
  --color-warning: #f59e0b;
  --color-danger: #ec4899;
}
```

---

## 🐛 Известные проблемы

### Решенные проблемы

- ✅ **Табличный вид не прокручивается** - Исправлено добавлением `flex flex-col` к родительским контейнерам
- ✅ **Кастомные колонки не обновляются** - Добавлен polling для custom columns
- ✅ **Участники не видят назначенные задачи** - Исправлена фильтрация на основе роли
- ✅ **WASM ошибки в консоли** - Настроены excludeVitePreset
- ✅ **Realtime подключения** - Переход на polling систему

### Текущие ограничения

- Email уведомления не настроены (требуется SMTP сервер)
- Realtime обновления через polling (каждые 3 сек)
- Максимальный размер файла вложений: 50MB

---

## 🔄 Деплой в продакшн

### Вариант 1: Vercel

```bash
# Установите Vercel CLI
npm i -g vercel

# Деплой
vercel
```

Не забудьте добавить переменные окружения в Vercel Dashboard.

### Вариант 2: Netlify

```bash
# Установите Netlify CLI
npm i -g netlify-cli

# Деплой
netlify deploy --prod
```

### Edge Functions

Edge Functions должны быть задеплоены через Supabase CLI:

```bash
supabase functions deploy server
```

---

## 🤝 Вклад в проект

Мы приветствуем любой вклад в развитие проекта!

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

---

## 📝 Лицензия

Этот проект создан в образовательных целях и доступен под лицензией MIT.

---

## 👏 Благодарности

- [Supabase](https://supabase.com) - за отличный BaaS
- [Shadcn/ui](https://ui.shadcn.com/) - за компоненты UI
- [Lucide](https://lucide.dev) - за иконки
- [Tailwind CSS](https://tailwindcss.com) - за CSS фреймворк
- [Figma Make](https://figma.com) - за платформу для прототипирования

---

## 📞 Контакты

Если у вас есть вопросы или предложения:

- 📧 Email: your.email@example.com
- 💬 GitHub Issues: [создать issue](https://github.com/yourusername/t24-task-manager/issues)

---

<div align="center">
  <p>Сделано с ❤️ для сообщества разработчиков</p>
  <p>⭐ Поставьте звезду, если проект был полезен!</p>
</div>
