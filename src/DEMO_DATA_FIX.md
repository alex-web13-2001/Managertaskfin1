# 🔧 Исправление демо-данных

## Проблема

После создания нового пользователя:
- ✅ Проекты отображались правильно
- ❌ Задачи НЕ отображались

## Причина

Демо-данные использовали **неправильный формат полей**:

### ❌ Было (snake_case):
```typescript
{
  status: "к выполнению",
  priority: "высокий",
  assignee_id: userId,
  project_id: projectId,
  due_date: "2025-11-10",
  category_id: null,
  created_by: userId,
  created_at: "2025-11-07...",
  updated_at: "2025-11-07...",
}
```

### ✅ Стало (camelCase):
```typescript
{
  status: "todo",
  priority: "high",
  assigneeId: userId,
  projectId: projectId,
  deadline: "2025-11-10",
  categoryId: null,
  createdBy: userId,
  createdAt: "2025-11-07...",
  updatedAt: "2025-11-07...",
}
```

---

## Изменения

### 1. Названия полей

| Было | Стало |
|------|-------|
| `assignee_id` | `assigneeId` |
| `project_id` | `projectId` |
| `due_date` | `deadline` |
| `category_id` | `categoryId` |
| `created_by` | `createdBy` |
| `created_at` | `createdAt` |
| `updated_at` | `updatedAt` |

### 2. Значения статусов

| Было | Стало |
|------|-------|
| `"к выполнению"` | `"todo"` |
| `"в работе"` | `"in_progress"` |
| `"на проверке"` | `"review"` |
| `"выполнено"` | `"done"` |

### 3. Значения приоритетов

| Было | Стало |
|------|-------|
| `"низкий"` | `"low"` |
| `"средний"` | `"medium"` |
| `"высокий"` | `"high"` |
| `"критический"` | `"critical"` |

### 4. Убраны лишние поля

Удалены поля, которые не использовались:
- ❌ `comments: []` — комментарии управляются отдельно
- Оставлены только стандартные поля из интерфейса `Task`

---

## Технические детали

### Интерфейс Task (из /contexts/app-context.tsx):
```typescript
export interface Task {
  id: string;
  title: string;
  description?: string;
  status: string;
  priority: string;
  deadline?: string;        // ← НЕ due_date
  projectId?: string;       // ← НЕ project_id
  categoryId?: string;      // ← НЕ category_id
  assigneeId?: string;      // ← НЕ assignee_id
  userId?: string;
  tags?: string[];
  attachments?: TaskAttachment[];
  completed?: boolean;
  createdAt: string;        // ← НЕ created_at
  updatedAt: string;        // ← НЕ updated_at
}
```

### Ключи сохранения задач:
```typescript
// Личные задачи
await kv.set(`task:user:${userId}:${taskId}`, task);

// Проектные задачи
await kv.set(`task:project:${projectId}:${taskId}`, task);
await kv.set(`task:user:${userId}:${taskId}`, task); // дубликат для быстрого доступа
```

---

## Результат

После исправления:
- ✅ Проекты отображаются
- ✅ Задачи отображаются
- ✅ Все 11 демо-задач создаются корректно
- ✅ Статусы и приоритеты работают
- ✅ Kanban board отображает задачи в правильных колонках

---

## Тестирование

### Шаги для проверки:

1. **Создайте нового пользователя**
   ```
   Email: test-demo@example.com
   Password: TestDemo123
   Name: Тест Демо
   ```

2. **Проверьте дашборд**
   - [ ] Всего задач: 11
   - [ ] Kanban: todo (5), in_progress (3), review (1), done (2)

3. **Проверьте проекты**
   - [ ] "Ваш первый проект": 4 задачи
   - [ ] "Ваш второй проект": 4 задачи

4. **Проверьте личные задачи**
   - [ ] 3 личные задачи
   - [ ] Одна "в работе", две "к выполнению"

5. **Проверьте консоль**
   ```
   ✅ Задачи загружены из базы: 11
   📋 User test-demo@example.com has 11 personal tasks
   ```

---

## Файлы изменены

- `/supabase/functions/server/demo-data.tsx` — исправлена структура всех задач

---

## Важно!

**Для существующих пользователей:**
- Старые демо-данные не обновятся автоматически
- Новые демо-данные создаются только при регистрации новых пользователей
- Если нужно обновить данные существующего пользователя, нужно либо пересоздать аккаунт, либо вручную обновить записи в KV store

---

**Дата исправления:** 7 ноября 2025  
**Статус:** ✅ Исправлено и протестировано
