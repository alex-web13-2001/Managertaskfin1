# 🤝 Руководство по внесению вклада в T24

Спасибо за интерес к развитию T24 Task Manager! Мы рады любому вкладу в проект.

---

## 📋 Содержание

- [Кодекс поведения](#кодекс-поведения)
- [Как я могу помочь](#как-я-могу-помочь)
- [Процесс разработки](#процесс-разработки)
- [Стиль кода](#стиль-кода)
- [Коммиты](#коммиты)
- [Pull Requests](#pull-requests)
- [Отчеты об ошибках](#отчеты-об-ошибках)
- [Предложения функций](#предложения-функций)

---

## 📜 Кодекс поведения

Участвуя в этом проекте, вы соглашаетесь соблюдать следующие правила:

- Уважайте других участников
- Будьте открыты к конструктивной критике
- Фокусируйтесь на том, что лучше для сообщества
- Проявляйте эмпатию к другим участникам

---

## 🎯 Как я могу помочь

### Отчеты об ошибках

Нашли баг? Создайте [issue](https://github.com/yourusername/t24-task-manager/issues) с:

- Описанием проблемы
- Шагами для воспроизведения
- Ожидаемым поведением
- Скриншотами (если применимо)
- Информацией о браузере/ОС

### Исправление ошибок

1. Найдите issue с меткой `bug`
2. Прокомментируйте, что берете его в работу
3. Создайте PR с исправлением

### Новые функции

1. Сначала создайте issue с предложением
2. Дождитесь обсуждения и одобрения
3. Реализуйте функцию в отдельной ветке
4. Создайте PR

### Улучшение документации

- Исправьте опечатки
- Добавьте примеры
- Улучшите объяснения
- Переведите на другие языки

### Code Review

Помогите проверить открытые Pull Requests:

- Тестируйте изменения
- Предлагайте улучшения
- Проверяйте стиль кода

---

## 🔄 Процесс разработки

### 1. Форк репозитория

```bash
# Нажмите Fork на GitHub, затем:
git clone https://github.com/YOUR_USERNAME/t24-task-manager.git
cd t24-task-manager
```

### 2. Создайте ветку

```bash
# Для новой функции
git checkout -b feature/amazing-feature

# Для исправления бага
git checkout -b fix/bug-description

# Для документации
git checkout -b docs/what-you-changed
```

### 3. Настройте окружение

```bash
npm install
```

Создайте `.env.local`:
```bash
VITE_SUPABASE_URL=your_url
VITE_SUPABASE_ANON_KEY=your_key
```

### 4. Разработка

```bash
# Запустите dev сервер
npm run dev

# Откройте http://localhost:5173
```

### 5. Тестирование

Перед коммитом проверьте:

- [ ] Приложение запускается без ошибок
- [ ] Все существующие функции работают
- [ ] Новая функция работает как ожидается
- [ ] Нет console.error в браузере
- [ ] Код соответствует стилю проекта

### 6. Коммит

```bash
git add .
git commit -m "feat: add amazing feature"
```

### 7. Push

```bash
git push origin feature/amazing-feature
```

### 8. Pull Request

1. Откройте PR на GitHub
2. Заполните шаблон PR
3. Дождитесь code review
4. Внесите правки при необходимости

---

## 💅 Стиль кода

### TypeScript

```typescript
// ✅ Хорошо
interface Task {
  id: string;
  title: string;
  status: string;
}

function createTask(data: Task): Promise<Task> {
  return api.post('/tasks', data);
}

// ❌ Плохо
function createTask(data: any): any {
  return api.post('/tasks', data);
}
```

### React Components

```tsx
// ✅ Хорошо - функциональный компонент с TypeScript
interface TaskCardProps {
  task: Task;
  onUpdate: (task: Task) => void;
}

export function TaskCard({ task, onUpdate }: TaskCardProps) {
  const handleClick = () => {
    onUpdate({ ...task, status: 'done' });
  };

  return (
    <div className="p-4 rounded-lg border">
      <h3>{task.title}</h3>
      <button onClick={handleClick}>Готово</button>
    </div>
  );
}

// ❌ Плохо - классовый компонент без типов
export class TaskCard extends Component {
  render() {
    return <div>{this.props.task.title}</div>;
  }
}
```

### Tailwind CSS

```tsx
// ✅ Хорошо
<div className="flex items-center gap-2 p-4 rounded-lg bg-purple-50">

// ❌ Плохо - инлайн стили
<div style={{ display: 'flex', padding: '16px' }}>
```

### Именование

```typescript
// Компоненты - PascalCase
TaskCard.tsx
ProjectModal.tsx

// Функции - camelCase
createTask()
updateProject()

// Константы - UPPER_SNAKE_CASE
const API_BASE_URL = '...';
const MAX_FILE_SIZE = 50 * 1024 * 1024;

// Файлы утилит - kebab-case
task-helpers.ts
date-utils.ts
```

### Комментарии

```typescript
// ✅ Хорошо - объясняет "почему"
// Используем polling вместо realtime из-за ограничений Supabase
useEffect(() => {
  const interval = setInterval(fetchTasks, 3000);
  return () => clearInterval(interval);
}, []);

// ❌ Плохо - объясняет "что" (и так видно из кода)
// Создаем интервал
const interval = setInterval(fetchTasks, 3000);
```

---

## 📝 Коммиты

Мы используем [Conventional Commits](https://www.conventionalcommits.org/).

### Формат

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Типы

- `feat` - новая функция
- `fix` - исправление бага
- `docs` - изменения в документации
- `style` - форматирование, отступы (не CSS)
- `refactor` - рефакторинг кода
- `perf` - улучшение производительности
- `test` - добавление тестов
- `chore` - обновление зависимостей, конфигурации

### Примеры

```bash
# Новая функция
feat(tasks): add drag and drop to kanban board

# Исправление бага
fix(auth): resolve token refresh issue

# Документация
docs(api): update endpoints documentation

# Рефакторинг
refactor(components): extract task card to separate component

# Стиль
style(header): fix alignment and spacing

# Performance
perf(tasks): optimize task filtering algorithm
```

---

## 🔀 Pull Requests

### Шаблон PR

```markdown
## Описание
Краткое описание изменений.

## Тип изменений
- [ ] Bug fix (исправление бага)
- [ ] New feature (новая функция)
- [ ] Breaking change (ломающие изменения)
- [ ] Documentation update

## Checklist
- [ ] Код соответствует стилю проекта
- [ ] Самостоятельно протестировал изменения
- [ ] Обновил документацию (если нужно)
- [ ] Нет новых warnings в консоли

## Скриншоты (если применимо)
[Добавьте скриншоты]

## Дополнительный контекст
Любая дополнительная информация.
```

### Процесс review

1. Maintainer проверит PR в течение 48 часов
2. Могут быть запрошены изменения
3. После одобрения PR будет смержен
4. Ваш вклад появится в CHANGELOG

### Автоматические проверки

При создании PR автоматически запустятся:

- [ ] ESLint проверка
- [ ] TypeScript compilation
- [ ] Build test

Все проверки должны быть зелеными ✅

---

## 🐛 Отчеты об ошибках

### Шаблон Issue для бага

```markdown
## Описание проблемы
Четкое описание того, что не работает.

## Шаги для воспроизведения
1. Перейти на '...'
2. Нажать на '...'
3. Прокрутить до '...'
4. Увидеть ошибку

## Ожидаемое поведение
Что должно было произойти.

## Текущее поведение
Что происходит на самом деле.

## Скриншоты
Если применимо, добавьте скриншоты.

## Окружение
- ОС: [например, macOS 14.0]
- Браузер: [например, Chrome 120]
- Версия приложения: [например, 1.0.0]

## Дополнительный контекст
Любая другая информация.

## Логи консоли
```
Вставьте логи из console.error
```
```

---

## 💡 Предложения функций

### Шаблон Issue для функции

```markdown
## Описание функции
Четкое описание предлагаемой функции.

## Проблема, которую решает
Объясните, какую проблему решает эта функция.

## Предлагаемое решение
Как вы видите реализацию этой функции.

## Альтернативы
Какие альтернативные решения вы рассматривали.

## Дополнительный контекст
Скриншоты, мокапы, ссылки на похожие реализации.
```

---

## 🏗 Архитектура проекта

### Структура компонентов

```
components/
├── ui/              # Базовые UI компоненты (button, input, etc)
├── *-view.tsx       # Страницы приложения
├── *-modal.tsx      # Модальные окна
├── *-board.tsx      # Kanban доски
└── *-table.tsx      # Табличные виды
```

### Управление состоянием

- Глобальное состояние → `app-context.tsx`
- Локальное состояние → `useState`
- Серверное состояние → polling через context

### API взаимодействие

Все API вызовы идут через App Context:
```typescript
const { tasks, createTask, updateTask } = useApp();
```

---

## 🧪 Тестирование

### Ручное тестирование

Перед созданием PR протестируйте:

1. **Регистрация/Вход**
   - Создайте нового пользователя
   - Войдите с существующим
   - Выйдите и войдите снова

2. **Задачи**
   - Создайте задачу
   - Отредактируйте задачу
   - Удалите задачу
   - Перетащите в Kanban

3. **Проекты**
   - Создайте проект
   - Пригласите участника
   - Проверьте права доступа

4. **Файлы**
   - Загрузите вложение
   - Загрузите аватар
   - Проверьте размер файла

5. **Responsive**
   - Откройте на мобильном (DevTools)
   - Проверьте все экраны

---

## 📚 Полезные ресурсы

### Документация

- [React Docs](https://react.dev)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [Supabase Docs](https://supabase.com/docs)

### Инструменты

- [React DevTools](https://react.dev/learn/react-developer-tools)
- [Tailwind CSS IntelliSense](https://marketplace.visualstudio.com/items?itemName=bradlc.vscode-tailwindcss)
- [ESLint](https://eslint.org/)

---

## ❓ Вопросы

Если у вас есть вопросы:

1. Проверьте [существующие issues](https://github.com/yourusername/t24-task-manager/issues)
2. Проверьте [документацию](README.md)
3. Создайте новый issue с меткой `question`

---

## 🙏 Благодарности

Спасибо всем, кто вносит вклад в проект! Ваши имена появятся здесь:

- [@yourname](https://github.com/yourname) - Feature X
- [@contributor](https://github.com/contributor) - Bug fix Y

---

**Happy Coding! 🚀**
