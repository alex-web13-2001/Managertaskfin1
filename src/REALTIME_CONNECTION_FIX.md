# Исправление проблем с Real-time подключением

## Проблемы

1. **У владельца проекта постоянно отображался статус "Offline"** - несмотря на то, что real-time подключение было активным
2. **У участников проекта отсутствовал индикатор онлайн/офлайн статуса**
3. **При каждом перемещении карточки показывался toast "Задача обновлена"** - что было раздражающим при активной работе с Kanban доской

## Причины

### 1. Проблема с таймаутом в app-context.tsx
В файле `/contexts/app-context.tsx` был установлен таймаут на 10 секунд, который ВСЕГДА устанавливал `isRealtimeConnected` в `false` независимо от реального статуса подключения:

```typescript
// ❌ НЕПРАВИЛЬНО
connectionCheckTimeout = setTimeout(() => {
  setIsRealtimeConnected(false);
  console.log('⚠️ Real-time подключение: timeout');
}, 10000);
```

Это означало, что даже если подписка успешно подключалась, через 10 секунд статус принудительно менялся на "Offline".

### 2. Скрытие индикатора на маленьких экранах
В файле `/components/header.tsx` индикатор был скрыт на экранах меньше `lg`:

```typescript
// ❌ НЕПРАВИЛЬНО
<div className="hidden lg:block">
  <RealtimeIndicator isConnected={isRealtimeConnected} />
</div>
```

### 3. Toast при каждом обновлении задачи
При каждом вызове `updateTask` показывался toast, включая при перемещении карточек в Kanban режиме.

## Исправления

### 1. Удален принудительный таймаут
В `/contexts/app-context.tsx` удален таймаут, который устанавливал статус в offline. Теперь статус подключения определяется только реальным статусом Supabase подписки:

```typescript
// ✅ ПРАВИЛЬНО - статус определяется только подпиской
.subscribe((status) => {
  console.log('🔴 Tasks subscription status:', status);
  if (status === 'SUBSCRIBED') {
    setIsRealtimeConnected(true);
    console.log('✅ Real-time подключено: Задачи');
  } else if (status === 'CHANNEL_ERROR' || status === 'TIMED_OUT' || status === 'CLOSED') {
    setIsRealtimeConnected(false);
    console.log('❌ Real-time ошибка: Задачи');
  }
});
```

### 2. Индикатор теперь отображается на всех экранах
В `/components/header.tsx` убрано ограничение по размеру экрана:

```typescript
// ✅ ПРАВИЛЬНО
<RealtimeIndicator isConnected={isRealtimeConnected} />
```

Также оптимизирован компонент индикатора для мобильных устройств:
- Меньший размер иконок на мобильных
- Текст скрывается на очень маленьких экранах (показывается только иконка)

### 3. Добавлен silent режим для обновления задач
В `/contexts/app-context.tsx` добавлен опциональный параметр `silent`:

```typescript
const updateTask = async (
  taskId: string, 
  updates: Partial<Task>, 
  options?: { silent?: boolean }
): Promise<Task> => {
  // ...
  if (!options?.silent) {
    toast.success('Задача обновлена');
  }
  // ...
}
```

Обновлены все Kanban компоненты для использования silent режима:
- `/components/kanban-board.tsx`
- `/components/personal-kanban-board.tsx`
- `/components/project-kanban-board.tsx`

```typescript
// ✅ ПРАВИЛЬНО
await updateTask(taskId, { status: newStatus }, { silent: true });
```

## Результат

✅ **Владелец проекта** теперь видит корректный статус подключения (Online при активном подключении)
✅ **Все участники** видят индикатор онлайн/офлайн статуса независимо от размера экрана
✅ **Перемещение карточек** работает плавно без раздражающих уведомлений
✅ **Real-time обновления** работают корректно для всех пользователей

## Как тестировать

1. **Проверка статуса подключения:**
   - Откройте приложение
   - В header должен отображаться зеленый индикатор "Online" с пульсирующей точкой
   - Откройте консоль браузера и проверьте логи: `✅ Real-time подключено: Задачи`

2. **Проверка многопользовательской работы:**
   - Откройте два браузера/вкладки с разными пользователями
   - Добавьте участника в проект
   - Переместите карточку в одной вкладке
   - Карточка должна автоматически переместиться во второй вкладке

3. **Проверка отсутствия спама toast:**
   - Активно перемещайте карточки между колонками
   - Toast "Задача обновлена" НЕ должен показываться при перемещении
   - Toast должен показываться только при явном редактировании задачи через модальное окно

## Дата исправления
3 ноября 2025
