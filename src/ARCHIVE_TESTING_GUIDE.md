# Руководство по тестированию системы архивирования проектов

## Что было реализовано

Полная система архивирования проектов с backend интеграцией:

### Backend API
- ✅ `POST /projects/:id/archive` - архивирование проекта (только владелец)
- ✅ `POST /projects/:id/restore` - восстановление проекта (только владелец)
- ✅ `GET /projects/archived` - получение всех архивных проектов
- ✅ `DELETE /projects/:id` - окончательное удаление (только из архива)

### Frontend
- ✅ Страница "Архив" полностью переработана для архивных проектов
- ✅ Навигация через меню профиля (Header)
- ✅ Кнопка "В архив" в списке проектов (только для владельцев)
- ✅ Кнопки "Восстановить" и "Удалить навсегда" в архиве

## Логика работы

```
Активный проект → В архив → Архив → Восстановить ИЛИ Удалить навсегда
                    (владелец)         (владелец)
```

### Важные особенности:

1. **Архивирование:**
   - Доступно только владельцу проекта
   - Проект архивируется со всеми задачами
   - Задачи остаются в базе, но скрыты из основных списков

2. **Восстановление:**
   - Проект возвращается в активные
   - Все задачи проекта снова доступны
   - Сохраняются все участники и настройки

3. **Удаление:**
   - Возможно только из архива
   - Удаляет проект безвозвратно
   - Требует подтверждения

## Как протестировать

### 1. Архивирование проекта

1. Авторизуйтесь в системе
2. Перейдите в раздел "Проекты"
3. Выберите проект, где вы владелец
4. Нажмите на "⋮" (меню действий)
5. Выберите "В архив"
6. Проект должен исчезнуть из списка активных

### 2. Просмотр архива

1. Кликните на аватар в правом верхнем углу
2. В выпадающем меню выберите "Архив"
3. Вы должны увидеть архивированные проекты
4. Проверьте отображение:
   - Название проекта
   - Категория (если есть)
   - Количество участников
   - Дата архивирования

### 3. Восстановление проекта

1. В разделе "Архив" найдите нужный проект
2. Нажмите кнопку "Восстановить"
3. Проект должен вернуться в активные
4. Перейдите в "Проекты" - проект должен там быть
5. Все задачи проекта должны быть доступны

### 4. Окончательное удаление

1. В разделе "Архив" найдите проект
2. Нажмите кнопку с иконкой корзины
3. Подтвердите удаление в диалоге
4. Проект должен исчезнуть из архива навсегда

### 5. Проверка прав доступа

**Владелец:**
- ✅ Может архивировать проект
- ✅ Может восстанавливать проект
- ✅ Может удалить проект из архива

**Участник с правами (Collaborator):**
- ❌ НЕ может архивировать проект
- ❌ НЕ видит кнопку "В архив" в меню

**Участник (Member) и Наблюдатель (Viewer):**
- ❌ НЕ могут архивировать проект
- ❌ НЕ видят кнопку "В архив" в меню

### 6. Поиск в архиве

1. В разделе "Архив" используйте поле поиска
2. Введите название проекта
3. Список должен фильтроваться в реальном времени

## Проверка консоли

При выполнении операций в консоли браузера должны появляться логи:

```
📦 Archiving project {projectId}
✅ Project archived successfully
```

```
📦 Restoring project {projectId} from archive
✅ Project restored from archive successfully
```

```
✅ Архивные проекты загружены: {count}
```

## Известные ограничения

1. **Задачи архивного проекта:** В текущей реализации задачи архивного проекта остаются в базе, но не показываются в интерфейсе. При восстановлении проекта они снова становятся доступны.

2. **Фильтрация в проектах:** В списке проектов есть фильтр "Архивированные", который пока показывает пустой список, так как архивные проекты теперь хранятся отдельно и доступны только через раздел "Архив".

## Что проверить дополнительно

- [ ] Архивирование проекта с задачами
- [ ] Восстановление проекта - задачи снова видны
- [ ] Удаление проекта из архива
- [ ] Права доступа (владелец vs участник)
- [ ] Поиск в архиве
- [ ] Отображение даты архивирования
- [ ] Корректная работа тостов (уведомлений)
- [ ] Работа кнопок при загрузке (disabled state)

## Возможные ошибки

Если что-то не работает:

1. **Проект не архивируется:**
   - Проверьте, что вы владелец проекта
   - Откройте консоль браузера для просмотра ошибок
   - Проверьте, что backend сервер запущен

2. **Архивные проекты не загружаются:**
   - Проверьте авторизацию (токен)
   - Откройте Network tab в DevTools
   - Убедитесь что запрос `/projects/archived` выполняется

3. **Ошибка прав доступа:**
   - Backend проверяет роль пользователя
   - Только владелец может архивировать/восстанавливать/удалять

## Интеграция с backend

Все операции с архивом работают через backend API:
- Хранение: KV Store (`project:user:{userId}:{projectId}`)
- Поле `archived: true` отмечает проект как архивный
- Поле `archivedAt` содержит дату архивирования
- Фильтрация происходит на уровне backend и frontend
